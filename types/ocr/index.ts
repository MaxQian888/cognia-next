/**
 * Boundary shim — the OCR type contracts moved into `@cognia/ocr` so the core
 * has zero app imports. Kept so existing `@/types/ocr` importers stay unchanged.
 */
export * from "@cognia/ocr/types"
