/**
 * @deprecated The canonical location moved to `types/agent/agent-trace.ts`
 * — re-exported here for backward-compat with existing import paths. Move
 * imports to `@/types/agent` (the barrel) or `@/types/agent/agent-trace`
 * directly.
 */

export * from "./agent/agent-trace"
