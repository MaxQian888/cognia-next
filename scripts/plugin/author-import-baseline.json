{
  "$comment": "Plugins that still import host-private modules instead of @cognia/plugin-sdk. This list may only shrink — see scripts/plugin/check-author-imports.mjs.",
  "notYetMigrated": ["workflow-ai"]
}
