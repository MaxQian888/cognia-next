/**
 * System hooks barrel.
 *
 * Tauri tray / menu / CLI / deep-link event subscription, mounted
 * once near the root via `tauri-provider.tsx`.
 */

export { useTauriEvents } from "./use-tauri-events"
