# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Next.js 16 (React 19) + Tauri 2.9 + Capacitor 8 + TypeScript + Tailwind v4 + shadcn/ui + Zustand. The same Next.js static export feeds three shells: the browser (`pnpm dev`), the Tauri desktop app (`pnpm tauri dev`), and the Capacitor mobile shell (`pnpm mobile:sync`).

## Working Rules (read before touching code)

These are project-level hard rules. They override any default behavior to the contrary.

> **Development pipeline:** the end-to-end flow that wires these rules to concrete skills/agents/gates (idea → brainstorm → grill → plan → build → preflight → gates) lives in [`WORKFLOW.md`](./WORKFLOW.md). Follow it for any non-trivial change.

1. **Research before implementing.** Before writing any new code, search `lib/`, `components/`, `hooks/`, `src-tauri/`, and the relevant ADR for an existing implementation. Reuse — don't reimplement. If you think you need a new utility/component/hook, first prove (with grep results or file paths) that no equivalent exists. The Subsystem Map and **Cross-cutting hooks** section below list the most reused entry points; check them first.
2. **No simplifications.** Implement the full behavior the task requires. Do not stub, mock-out, abbreviate, or "// TODO later" production paths. Do not strip error handling, validation, or edge cases to ship faster. If something genuinely cannot be implemented now, stop and surface the blocker — do not silently degrade scope.
3. **Every component ships with a unit test.** Any new file under `components/**`, `hooks/**`, `lib/**`, `src-tauri/src/**`, or the marketing workspace's `web/{components,hooks,lib}/**` (excluding `components/ui/`, `components/ai-elements/`, and `web/components/ui/`) must have a co-located `*.test.ts(x)` / in-file `#[cfg(test)]` test — enforced by `pnpm audit:colocated-tests`, which fails on any new or renamed source file without one. Editing an existing component? Update or add tests in the same change.
4. **Every frontend component is i18n-wired.** No hard-coded user-facing strings in `.tsx`. Use `useTranslations()` / `getTranslations()` from `next-intl`, add the new keys to **both** `i18n/messages/en.json` and `i18n/messages/zh-CN.json`, and run `pnpm lint:i18n` to confirm parity with the baseline. Aria labels, placeholders, toasts, and error messages count as user-facing.
5. **Language convention.** Internal narration (status updates, tool-call rationale, end-of-turn summaries, code comments) is written in **English**. Questions to the user — clarifications, `AskUserQuestion` prompts, confirmation requests — are written in **Chinese**.
6. **Record a changeset for every user-facing change.** After implementing a feature, fix, or behavior/breaking change that a user would notice, run `pnpm changeset` — select the **`cognia-next`** package, pick the semver bump (`patch` fix, `minor` feature, `major` breaking), and write a one-line summary. This creates a `.changeset/*.md` file you commit alongside the code. Skip it only for internal-only work (tests, refactors, docs, chore, CI). See **Versioning & Release** below for the full model.
7. **Label intentional dormancy on all three axes.** Intentional dormancy must be documented at the type AND labeled inert in the UI AND pinned by a test. Any two of three is a latent bug.
8. **The working tree is shared — only ever touch your own changes.** Several Claude/agent sessions edit this checkout at the same time. Every git command you run must name explicit paths that _this_ task modified.
   - **Commit**: `git commit --only <your paths>` (or `git add <your paths> && git commit`). Never `git add .`, `git add -A`, `git add -u`, `git commit -a`, and never stage a whole directory to "pick up" a file you did not write.
   - **Never run tree-wide state commands**: `git stash` (any form, including `-u`/`pop`/`apply`/`drop`), `git checkout -- .`, `git checkout <ref> -- .`, `git restore .`, `git reset --hard`, `git clean`, `git rebase`, `git merge`, `git revert`, branch switches. These silently destroy other sessions' uncommitted work — the stash stack is shared, and `--hard`/`checkout .` erases edits that were never committed anywhere and cannot be recovered.
   - **Need a clean tree?** Don't make one. Read a pristine version with `git show HEAD:<path>`, diff a single file with `git diff -- <path>`, and if you truly need isolation, create a separate worktree (`git worktree add`) instead of resetting this one.
   - **Before committing**, re-check `git diff --cached --name-only` and drop anything you did not author this session. A file you merely _saw_ fail is not yours to fix or revert.
   - **If a gate fails on someone else's file**, report it as pre-existing and move on. Do not "fix" or revert in-flight work belonging to another session.
   - **`--no-verify` stays banned** (see Commit Hooks); a failed hook means fix your own files and make a new commit.

## Development Commands

```bash
# Frontend (port 3000)
pnpm dev / build / start / lint / lint:fix / format / format:check / typecheck

# Jest tests (co-located *.test.ts(x)); two projects: pure-.ts suites under
# lib/stores/cli/packages/types/plugins/i18n run in the fast `node` env,
# everything else (and any `/** @jest-environment jsdom */`-docblocked .ts
# file — required for Dexie/getDb, localStorage, window stubs) runs in jsdom
pnpm test / test:watch
pnpm test:changed            # only suites affected by your diff vs origin/dev

# Playwright E2E (tests/e2e/; chromium+mobile run file-parallel, tauri stays serial)
pnpm test:e2e                # against the Turbopack dev server (60s test budget)
pnpm test:e2e:build          # NEXT_PUBLIC_E2E=1 static export → out/
pnpm test:e2e:static         # against the prebuilt out/ (fast, no per-route compiles; run test:e2e:build first)
pnpm test:e2e:changed        # only specs affected by your diff vs origin/dev

# Tauri desktop
pnpm tauri dev / build / info

# Capacitor mobile (run after `pnpm build` so out/ is fresh)
pnpm mobile:sync / mobile:open:ios / mobile:open:android

# Docs (Fumadocs, port 3001)
pnpm docs:dev / docs:build / docs:start

# Marketing website (port 3002) — ADR-0092. Uses the `@web/*` alias, NOT `@/`:
# root Jest maps `^@/` to the repo root and collects web/** tests, so a
# workspace-local `@/` would resolve against the wrong tree.
pnpm web:dev / web:build / web:start
pnpm web:evidence      # rebuild the release/changelog/stats snapshot
pnpm web:capture       # re-shoot the product screenshot matrix
pnpm web:test:scripts  # node --test over web/scripts/

# Repo-specific gates
pnpm audit:slots        # plugin slot manifest audit
pnpm lint:i18n          # diff against the i18n key baseline
pnpm lint:i18n:baseline # rewrite the i18n baseline after intentional changes
pnpm sidecar:test       # node --test on sidecar/builtin-tools/
pnpm build:packages     # standalone tsup build of the zero-`@/` packages
                        # (provider-types, memory, ocr). Run this after ANY
                        # types-shim extraction: a child tsconfig's `paths`
                        # REPLACE the root's, so a package can lose a path the
                        # root gates still resolve. `@cognia/plugin-sdk` is NOT
                        # in this gate — it imports ~390 `@/` app paths and
                        # cannot build standalone yet.

# shadcn/ui
pnpm dlx shadcn@latest add <component>

# Storybook (isolated component preview — additive, never replaces Jest)
pnpm storybook        # dev server on :6006
pnpm build-storybook  # static build (catches Vite alias gaps)

# Run a single Jest file
pnpm test -- path/to/file.test.ts
```

**Storybook** (`@storybook/nextjs-vite`, config in `.storybook/`): co-located `*.stories.tsx` for
isolated component dev/preview. Stories are **not tests** — they do not satisfy the co-located-test
rule and are excluded from `lint:i18n`. The Storybook AI MCP server (`storybook`
entry in `.mcp.json`, `@storybook/addon-mcp`) is only reachable **while `pnpm storybook` is
running** — use its MCP tools to author/preview stories, not as an always-on server.

`predev` / `prebuild` run `scripts/copy-monaco-assets.mjs` automatically — don't import Monaco assets manually.

## Architecture

### Workspaces (`pnpm-workspace.yaml`)

| Package  | Path      | Port | Mode                                                         |
| -------- | --------- | ---- | ------------------------------------------------------------ |
| Main app | `/`       | 3000 | static export (`out/`) — consumed by Tauri **and** Capacitor |
| Docs     | `docs/`   | 3001 | static export (`docs/out/`, Fumadocs) — Cloudflare Pages     |
| Website  | `web/`    | 3002 | static export (`web/out/`) — Cloudflare Pages (ADR-0092)     |
| Mobile   | `mobile/` | —    | Capacitor 8 shell over `../out`                              |

Install from repo root only — single `pnpm-lock.yaml`.

`sidecar/` is **not** in the workspace: it is a separate Node project (`cognia-claude-sidecar`) with its own `package.json` and lockfile. The root `pnpm sidecar:test` script just shells into it.

### Main app

- `app/` Next.js App Router (static-exported — `app/api/` does **not** exist at runtime)
- `components/ui/` — 57 pre-installed shadcn/ui components (**no tests**); `TooltipProvider` already mounted in `app/layout.tsx`
- `components/ai-elements/` — vendored (**no tests**)
- `hooks/`, `lib/utils.ts` (`cn()` = clsx + tailwind-merge)
- `i18n/` next-intl wiring (`request.ts`, `config.ts`, `messages/{en,zh-CN}.json`) — `i18n.request.ts` is plugged in via `withNextIntl(...)` in `next.config.ts`
- `plugins/` in-tree first-party plugins (`computer-use`, `clipboard-history`, `screenshot`, `web-tools`, …) — loaded by the plugin manager, not as npm packages
- `sidecar/` Node sidecar bundled by Tauri (`claude-host.mjs`, `dispatch/`, `builtin-tools/`, `fetch-interceptor.mjs`); reads `CLAUDE_CODE_OAUTH_TOKEN` from keyring

### Docs (`docs/`)

- Import conventions: `@/lib/source` (NOT `@/app/source`); `collections/server` (tsconfig alias → `.source/`); `fumadocs-ui/provider/next`
- `docs/.source/` auto-generated — run `pnpm docs:dev` or `pnpm docs:build` once so TS can resolve `collections/server`

### Tauri

- `src-tauri/` Rust backend; `tauri.conf.json` → `frontendDist: ../out`
- `beforeDevCommand`: `pnpm dev`; `beforeBuildCommand`: `pnpm build`

### Styling

- Tailwind v4 via `@tailwindcss/postcss`; oklch CSS vars in `globals.css`
- Dark mode: class-based; `@custom-variant dark (&:is(.dark *))`

### Path Aliases

`@/components` `@/lib` `@/utils` `@/ui` `@/hooks`

## Code Patterns

```tsx
import { cn } from "@/lib/utils"
cn("base", condition && "conditional", className)

<Button asChild><Link href="/x">Click</Link></Button>

import { greet, isTauri } from "@/lib/tauri"
if (isTauri()) greet("World").then(console.log)
```

## Subsystem Map

One line per subsystem — the **full detail lives in the ADR** under `docs/content/docs/en/adr/`. Read the ADR before any non-trivial change; use `Lives in` for reuse lookups.

| Subsystem                       | Lives in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Schema                                                                                                      | ADR                                                  |
| ------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- |
| Data backup / transfer          | `lib/data/`, `components/settings/data/`, `lib/db/backup-history.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                                                           | 0001                                                 |
| Employee Digital Twin           | `lib/twin/`, `types/twin/`, `components/twin/`, `app/twin/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | v14                                                                                                         | 0003                                                 |
| External Bridge                 | `lib/external-bridge/`, `lib/wiki/`, `crates/cognia-mcp-server/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | v17                                                                                                         | 0008                                                 |
| Platform Connectors             | `lib/connectors/`（跨壳写入面 `lib/connectors/inbox-writes/`、中继事件 `lib/connectors/inbox-relay/`）, `lib/a2ui/connector-callback-handler.ts`, `lib/sync/host-invalidate.ts`, `lib/companion/host-event-publisher.ts`, `app/inbox/`, `components/inbox/`, `crates/cognia-connectors/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | v173                                                                                                        | 0009, 0025, 0036, 0089, 0131                         |
| Unified Subscription            | `lib/subscription/`, `src-tauri/src/subscription/`, `components/settings/subscription/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | keyring v2 / v20                                                                                            | 0025                                                 |
| Visual Workflows                | `lib/workflow/`, `types/workflow/visual.ts`, `components/workflow/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | v22                                                                                                         | 0011                                                 |
| Scheduled task scheduler        | `lib/scheduler/` (`SchedulerDatabase` is a facade over the ACCOUNT db's `scheduledTasks`/`scheduledTaskRuns`, not its own database; policy gate `write-authority.ts`, host gates `host-support.ts`, managed-schedule target `scheduler-host-target.ts`), agent tools `lib/skills/built-in/scheduler/`, `types/scheduler/`, `components/scheduler/`, `app/scheduler/`, `crates/cognia-scheduling/`, backup legs `lib/data/destinations/`                                                                                                                                                                                                                                                                                                                                                                                                                                          | Dexie v219                                                                                                  | 0002, 0079, 0128, 0167                               |
| Plugin Dexie Tables             | `lib/plugin/dexie/`, `lib/plugin/api/dexie-api.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | v27                                                                                                         | dexie-tables.mdx                                     |
| Marketplace Integrations        | `lib/integrations/`, `lib/db/integrations.ts`, `types/plugin/plugin-integration.ts`, `lib/plugin/api/integrations-api.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Dexie v127                                                                                                  | 0026                                                 |
| Agent Team PR feedback          | `lib/github/pr-observe/`, `lib/github/octokit-factory.ts`, `lib/github/auth-app.ts`, `lib/ai/agent/team/pr-feedback/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | —                                                                                                           | 0022                                                 |
| Plugin workspace backends       | `lib/github/workspace.ts`, `lib/github/workspace-backend-registry.ts`, `lib/plugin/api/workspace-api.ts`, `plugins/e2b-sandbox/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —                                                                                                           | 0026                                                 |
| Capacitor mobile                | `mobile/`, `lib/capacitor/`, `components/mobile/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                                                                                           | 0014, 0015                                           |
| /goal Command                   | `lib/goal/`, `components/goal/`, `lib/slash-commands/actions/goal.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | v30                                                                                                         | 0019                                                 |
| Computer Use                    | `crates/cognia-automation/`, `lib/automation/`, `components/automation/`, `plugins/computer-use/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | v32                                                                                                         | 0020                                                 |
| Embedded browser                | `lib/browser/`, `components/browser/`, `hooks/browser/`, `src-tauri/src/browser/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | local / WK cookie store                                                                                     | 0055, 0072, 0073                                     |
| WebRTC WAN transport            | `services/signaling-server/`, `lib/signaling/`, `lib/tauri/transport-rtc.ts`, `components/settings/companion/webrtc-card.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | v33                                                                                                         | 0021                                                 |
| OCR subsystem                   | `lib/ocr/`, `lib/db/ocr-results.ts`, `lib/slash-commands/actions/ocr.ts`, `components/settings/ocr/`, `hooks/use-ocr.ts`, `plugins/ocr/`, `crates/cognia-ocr/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | v36                                                                                                         | 0024                                                 |
| Mobile sync orchestrator        | `lib/sync/`, `hooks/data/use-dexie-first-query.ts`, `lib/connectivity/`, `lib/capacitor/`, `components/mobile/`, `app/{sw,manifest}.ts`, `lib/db/session-attachment-uploads.ts`, `lib/companion/attachment-upload-client.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | v46 / v180（宿主端 `sessionAttachmentUploads`中转）                                                         | 0027                                                 |
| Agent Team                      | `lib/ai/agent/`, `lib/claude/agents/subagents/`, `lib/plugin/registries/`, `components/agent/workspace/`, `stores/agent/agent-team-store/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | v4 persist                                                                                                  | 0002, 0022, 0032                                     |
| Unified Plan Execution Hub      | `types/agent/plan.ts`, `lib/agent/plan/`（`runtime`/`strategy`/`turn-driver`/`step-dispatch`/`synthesize-workflow`/`workflow-conversion`/`projections`/`agent-tool-capture`/`exit-plan-capture`/`step-params`/`notify`）、`lib/db/plans.ts`、`components/agent/plan/`、`lib/workflow/nodes/plans/`（15 个 `action.plan.*` 执行器）、`lib/slash-commands/actions/plan.ts`（/plan 8 个子命令）、`sidecar/builtin-tools/plan-tools.mjs`（`create_plan`/`update_plan`）、`lib/plugin/api/plan-api.ts`、`lib/sync/handlers/plans.ts`                                                                                                                                                                                                                                                                                                                                                  | Dexie v71（`agentPlans` + `agentPlanEvents`）                                                               | 0045                                                 |
| Unified Agent execution         | `lib/ai/agent/execution/`, `lib/ai/agent/recovery/`, `sidecar/`, `lib/ai/agent/external/`, `lib/gateway/`, `packages/agent-config-types/`, `crates/cognia-gateway/`, `tests/conformance/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | v121/v123/v124 + frozen execution spec                                                                      | 0090                                                 |
| Agent SDK runtime client        | `packages/agent/`, `cli/src/agent/rpc/`, `packages/agent-host-{darwin-arm64,linux-x64,win32-x64}/`, `scripts/build/package-agent-host.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | SDK v0.1 runtime client / v0.2 immutable agent definitions                                                  | 0090, 0142                                           |
| Integrated terminal             | `lib/terminal/`, `components/terminal/`, `stores/terminal/`, `crates/cognia-terminal/`, `src-tauri/src/companion_api/ws_terminal.rs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | persist                                                                                                     | 0031, 0033                                           |
| CLI ↔ App bridge                | `lib/cli-bridge/`, `src-tauri/src/cli_bridge/`, `cli/src/handoff/`, `components/settings/cli-bridge/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | forked stores / shared code                                                                                 | 0078                                                 |
| Rust performance dashboard      | `src-tauri/src/perf/`, `lib/perf/backend/`, `hooks/perf/use-perf-stream.ts`, `components/performance/`, `app/performance/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                           | 0035                                                 |
| Public share links              | `lib/share/`, `lib/db/shared-links.ts`, `components/share/`, `app/share/view/`, `services/share-server/worker/`, `services/share-server/pages/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | v54                                                                                                         | 0037                                                 |
| Desktop pet                     | `components/pet/`, `lib/pet/`, `hooks/pet/`, `stores/pet/`, `types/pet/`, `src-tauri/src/pet_window/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Dexie (no schema bump)                                                                                      | 0058                                                 |
| Web reader                      | `lib/web/reader/`, `lib/web/web-tools-core.ts`, `packages/document/.../html-parser.ts`, `lib/twin/ingest/url-fetcher.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | —                                                                                                           | 0060                                                 |
| Wiki lint                       | `lib/wiki/lint/`, `lib/db/wiki-lint-results.ts`, `components/settings/external-bridge/wiki-lint-card.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Dexie v95                                                                                                   | 0060                                                 |
| Attention Radar                 | `lib/radar/`, `types/radar/`, `lib/db/radar-reports.ts`, `components/pet/console/radar-panel.tsx`, `hooks/pet/use-pet-insight.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Dexie v96                                                                                                   | 0060                                                 |
| Content capture                 | `lib/capture/`, `types/capture/`, `components/capture/`, `stores/capture/`, `hooks/capture/`, `lib/db/captured-items.ts`, `src-tauri/src/capture/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Dexie v97                                                                                                   | 0060                                                 |
| Agent session import            | `lib/session-import/`, `hooks/session-import/`, `components/session-import/`, `src-tauri/src/session_import.rs`, `lib/plugin/api/import-api.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | reuses sessions/messages                                                                                    | 0062                                                 |
| Coding-agent migration          | `lib/agent-migration/`, `lib/settings-import/`, `lib/commands-import/`, `components/agent-migration/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | delegates to existing stores                                                                                | 0107                                                 |
| Optical compaction              | `sidecar/dispatch/optical/`, `sidecar/dispatch/compaction*.mjs`, `lib/db/optical-archives.ts`, `lib/claude/optical-archive-persist.ts`, `components/chat/message-parts/optical-archive-dialog.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Dexie v101                                                                                                  | 0063                                                 |
| Long-term memory                | `lib/memory/`, `packages/memory/`, `types/memory/`, `lib/db/memories.ts`, `components/memory/`, `app/memory/`, `lib/plugin/api/memory-api.ts`, `lib/external-bridge/handlers/memory.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Dexie v65                                                                                                   | 0069                                                 |
| Risk→ceremony policy            | `lib/policy/risk/`, `lib/ai/agent/team/risk-input.ts`, gate wiring in `lib/ai/agent/agent-team-runtime.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —                                                                                                           | 0070                                                 |
| Voice / TTS                     | `packages/tts/`, `crates/cognia-tts/`, `lib/tts/`, `components/settings/speech/`, `app/me/speech/`, pet voice in `hooks/pet/use-pet-speak.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Dexie tts-cache                                                                                             | 0075                                                 |
| 源代码管理 (SCM)                | `crates/cognia-git/`, `components/source-control/`, `lib/git/`, `stores/git/`, `hooks/git/`, `app/source-control/`, settings `components/settings/source-control/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | `AppSettings.gitSettings.panel` (非 Dexie)                                                                  | 0038                                                 |
| 分支栈 (stacked PR)             | `lib/stack/`（`model`/`topology`/`validate`/`restack`/`publish`/`merge`/`discover`/`team-policy` + `lib/stack/forge/` 适配器接缝）、`crates/cognia-git/src/stack.rs`（父指针 / 祖先校验 / `git replay` / 带租约强推 / `refs/cognia/stack-history/*`）、`components/source-control/stack-panel.tsx`、`hooks/git/use-stacks.ts`、工作流 `lib/workflow/nodes/source-control/stack.ts`（`action.stack.*`）、CLI `cli/src/tui/runtime/stack-controller.ts`（/stack）                                                                                                                                                                                                                                                                                                                                                                                                                  | 无（git config `branch.<name>.cognia-parent` 即真相源）                                                     | 0151                                                 |
| 仓库供给与对象缓存              | `crates/cognia-task-workspace/src/mirror.rs`（每 remote 一个裸镜像 + TTL + commit-graph/MIDX + 按龄回收）、`crates/cognia-task-workspace/src/snapshot.rs`（只存 git 不掌握的部分）、`crates/cognia-git/src/repo.rs`（`clone_repo_guarded`，blobless 默认）、`src-tauri/src/github/workspace.rs`（从镜像派生）、`lib/workspace/{provisioning-inference,pnpm-virtual-store}.ts`、`lib/task-workspace/workspace-provisioning.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 无（磁盘缓存 + `Project.workspaceProvisioning` 同意记录）                                                   | 0150                                                 |
| Pro IDE (code-server)           | `src-tauri/src/codeserver/`, `lib/codeserver/`, `hooks/codeserver/`, `components/editor/project/{code-server-pane,editor-engine-toggle}.tsx`, `components/settings/pro-ide/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 无（磁盘 + settings.json）                                                                                  | 0088                                                 |
| 官网 (marketing site)           | `web/` (独立 workspace，`@web/*` alias，零跨包引用)；文案 `web/content/{types,en,zh}.ts`；证据管道 `web/scripts/build-evidence.mjs`；截图/OG `web/scripts/capture-*.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 无（构建期 JSON 快照）                                                                                      | 0092                                                 |
| 系统级划词工具栏                | `src-tauri/src/selection_toolbar.rs`, `components/selection-toolbar/`, `lib/tauri/selection-toolbar.ts`, `lib/selection/`, `lib/tts/speak-selection.ts`, `components/providers/initializers/selection-toolbar-initializer.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | 无（Tauri store pref + 渲染端测量窗口）                                                                     | 0093, 0095                                           |
| 桌面选区感知                    | `crates/cognia-automation/src/automation/selection_events.rs`（元数据总线）、`crates/cognia-automation/src/automation/platform/ax/observer.rs`（AXObserver）、`platform/shared/{screen_capture,screenshot}.rs`（录屏权限探测 + 全局矩形→显示器像素）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 无（进程内总线）                                                                                            | 0095                                                 |
| 会话锚点 / 跳转 / 永久链接      | `lib/chat/{message-anchor,message-permalink,cross-session-jump}.ts`, `hooks/chat/{use-jump-flash,use-jump-history,use-message-permalink}.ts`, `components/chat/{conversation-jump-pill,jump-flash,branch-lineage-chip}.tsx`, `components/chat/minimap/`, `stores/chat/chat-viewport-store.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | 无（viewport store，非持久化）                                                                              | 0094                                                 |
| 原生本地视频处理                | `crates/cognia-media/`（FFmpeg/FFprobe 发现、探测、抽帧、分析、去重、裁剪）；渲染端 `lib/plugin/api/media-api.ts`；命令注册 `src-tauri/src/lib.rs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | 无（磁盘 + 进程内 MediaSourceRegistry）                                                                     | —                                                    |
| 技能录制器                      | `lib/skills/recording/`（state-machine/step-model/controller/envelope/persist）、`stores/skills/recorder-store.ts`、`hooks/skills/use-skill-recorder.ts`、`components/skills/recorder/`、`lib/db/skill-recordings.ts`、原生 `crates/cognia-automation/src/automation/record/` + `src-tauri/src/recorder_window/`、插件 `plugins/skill-recorder/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Dexie v141（1 张表，仅本机；捕获在原生产物中）                                                              | 0106, 0020                                           |
| 统一模板平台                    | `lib/templates/`（contracts/catalog/service/runtime）、`lib/db/template-platform.ts`、`components/templates/`、`app/templates/`、`hooks/use-template-catalog.ts`、插件面 `lib/plugin/api/templates-api.ts`、启动 `components/providers/initializers/template-platform-initializer.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Dexie v132（5 张表，其中 2 张仅本机）                                                                       | 0100                                                 |
| 首次运行引导                    | `lib/onboarding/`、`components/onboarding/`、`app/onboarding/`、`components/providers/onboarding-gate.tsx`、`hooks/onboarding/`、内置 skill `skills/built-in/cognia-onboarding/`；入口 `components/settings/onboarding/`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | 无（`AppSettings.onboardingProgress` / `onboardingProfile`，state v2）                                      | 0122, 0141                                           |
| 问题反馈 / 支持报告             | `lib/support-report/`（sections + channels 注册表、`buildSupportReport` 脱敏门、`issue-url`）、`components/support/`（`ReportProblemDialog`、根挂载 `ReportProblemHost`（消费 `useUIStore().pendingReportRequest`，托盘 report-issue / /report 走此）、`SupportAgentPanel`、`SupportDiagnosticsConsent`）、`hooks/support/`、`lib/support-agent/`（Support Agent 上下文 + 本机诊断 kill switch）；通知中心 `diagnostic.*` 执行器 `lib/diagnostics/notification-commands.ts`                                                                                                                                                                                                                                                                                                                                                                                                      | 无（localStorage kill switch）                                                                              | 0042, 0102                                           |
| Issue 追踪器 / 执行桥           | `types/issues/`、`lib/db/{issues,issue-projects,issue-events,issue-counters,issue-runs,labels,github-issue-mirror}.ts`、`lib/issues/**`（`state-machine` 人类迁移 / `lib/issues/run/` `IssueRunAdapter` 注册表 + agent-task / agent-team / github-loop 三个 adapter / `lib/issues/sources/` 只读联邦源 / `notify.ts` 通知中心投影 / `lib/issues/im/` A2UI 卡片 + `issue_action` 回调 + 建单）、`components/issues/**`（`assignee-picker`、`run-issue-dialog`）、`components/workspace/workspace-overview.tsx`、`lib/global-search/providers/issues.ts`、内置 skill `lib/skills/built-in/issues/`；启动 `components/providers/initializers/issue-tracker-initializer.tsx`                                                                                                                                                                                                         | Dexie v170 / v171 / v174（`issueRuns`）                                                                     | 0132                                                 |
| 终端会话分享                    | `crates/cognia-terminal/src/host.rs`（`SessionParticipant` 名单广播）、`src-tauri/src/terminal_host_bridge.rs`、`lib/terminal/{base-session,session,transport-ws,session-registry}.ts`（`applySessionSnapshot` / `onInfo`）、`lib/terminal/collaboration/roster.ts`、`hooks/companion/use-remote-terminal-grant.ts`（与设备控制台 Access 页共用授权流）、`components/terminal/{terminal-share-dialog,terminal-session-chip}.tsx`                                                                                                                                                                                                                                                                                                                                                                                                                                                 | 无（宿主实时名单，不持久化）                                                                                | 0133, 0031, 0033                                     |
| 统一全局搜索 (⌘K)               | `lib/global-search/`（types / query-parser / registry / scoring / cache / recents / engine + `providers/*` 19 个内置 provider）、`hooks/global-search/`（context 装配、搜索 hook、动作执行）、`components/global-search/`（`GlobalSearchDialog` + tabs / chips / row / footer / empty-state）；薄适配器 `components/desktop/command-palette.tsx`、`components/mobile/home/mobile-command-palette.tsx`；打开接缝 `lib/shell/command-palette-request.ts`（`requestCommandPalette({query, scope})`）；快捷键 `app.commandPalette.toggle`（`lib/shortcuts/app-catalog.ts`）；消息引擎沿用 `lib/chat/search/`                                                                                                                                                                                                                                                                         | 无（localStorage recents）                                                                                  | 0129                                                 |
| 云端文档提供方 (@文件)          | `lib/docs-providers/`（`types`/`registry`/`limits`/`csv`/`oauth-deep-link` + `providers/{lark,google}/`）、鉴权 harness `lib/connectors/adapters/lark/authed-api.ts`、`hooks/chat/use-remote-doc-{search,staging}.ts`、`components/chat/composer-popover.tsx` 的 `@lark:`/`@gdoc:` 面板、`components/settings/connections/docs-providers/`、Rust 回环回调 `crates/cognia-connectors/src/axum_app.rs` `/oauth/docs/{provider}/callback`                                                                                                                                                                                                                                                                                                                                                                                                                                           | 无（keyring + 临时附件）                                                                                    | 0134                                                 |
| 诊断服务 / 崩溃上报             | 服务端 `services/diagnostic-server/`（独立 Cargo 工程，自带 lockfile 与镜像）；共享上传状态机 `crates/cognia-observability/src/diagnostic_submit.rs`；桌面打包上传 `crates/cognia-observability/src/crash/submit.rs` + `crates/cognia-observability/src/crash/receipts.rs`；客户端 `lib/diagnostic-service/`；平台传输 `lib/network/platform-fetch.ts`（与 `lib/server-ops/` 共用）；界面 `components/logging/incident-workspace.tsx`、`components/logging/service-console-workspace.tsx`、`hooks/diagnostic-service/`、`components/settings/sections/diagnostic-service-card.tsx`；报告通道 `lib/support-report/diagnostic-channel.ts`；CLI `crates/cognia-cli/src/commands/crash.rs`                                                                                                                                                                                           | 服务端 Postgres（迁移 0001–0006）；本机仅 keyring + crash-reports 侧车                                      | 0102, 0135                                           |
| 桌面网络代理                    | `crates/cognia-net/src/proxy_config/`（策略/绕过/校验/`managed_client`/`wsproxy`）、`src-tauri/src/proxy_config/`（`commands.rs` + `stream.rs` 流式桥）、`lib/network/`（`proxy-fetch` / `platform-fetch` / `platform-streaming-fetch` / `platform-websocket` / `sse-reader` / `desktop-network-runtime`）、`components/settings/network/`；门禁 `scripts/gates/check-network-egress.mjs`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 无（`AppSettings.networkProxy` + keyring 凭据）                                                             | —                                                    |
| 设备控制台                      | `lib/devices/`（`types` / `build-device-rows` / `capability-cells` / `grant-capabilities` / `device-runtime` / `placement-directory`，全纯函数）、`components/devices/`（`device-console` + `device-detail` 单栏仪表盘 + `device-hero` 统计条 + `device-section` 卡片原语 + `components/devices/sections/` 五个区块 + `device-visuals` 共享词表 + `host-controls`）、`hooks/devices/`（`use-device-rows` 轮询装配、`use-device-grant-actions` 授权写入、`use-device-options` 选择器候选）、`stores/devices/device-console-store.ts`、`app/devices/`；⌘K `lib/global-search/providers/devices.ts`；设置侧入口 `components/devices/device-console-link.tsx`                                                                                                                                                                                                                        | 无（Dexie `pairedDevices` + 宿主 SecurityStore + remote-host store 的投影）                                 | 0143, 0136                                           |
| 服务器运维 (Ops Controller)     | `lib/server-ops/`（`client` + `transport` 平台传输、`deployment-target`/`deployment-form`/`deployment-renderer`、`agent-enrollment`、`operation-stream`）、`components/servers/`、`app/servers/`（列表 + `detail?id=` 静态路由）、原生流 `crates/cognia-net/src/sse_stream.rs` + `src-tauri/src/server_ops.rs`（`server_ops_events_*`）、控制器 `crates/cognia-ops-controller/`、Agent `crates/cognia-deploy-agent/`、部署清单 `deploy/{controller,agent,compose,k8s}/`                                                                                                                                                                                                                                                                                                                                                                                                          | 无（控制器侧 Postgres；本机仅 keyring + 摘要离线缓存）                                                      | 0059                                                 |
| 工作区（多项目 × 多会话）       | `lib/workspace/`（`capability-overlay` 能力叠加、`panel-follow` 面板跟随、`session-root` 会话执行根唯一解析、`locate-workspace`/`adopt-candidates` 目录收编、`projects-root`/`create-workspace` 新建、`effective-cwd`、`repo-declared` 仓库声明播种）、`lib/chat/aggregate-run-state.ts`（全应用运行态）、`lib/execution/slot-key.ts` + broker 执行位串行、`lib/global-search/workspace-scope.ts`、`lib/scheduler/task-workspace-binding.ts`、`components/workspace/`、`components/chat/background-runs-chip.tsx`、`hooks/workspace/`；仓库配置 `lib/project-environment/`（`workspace-config` 解析、`workspace-config-trust` 信任+内容摘要门、`resolve-environment` 运行期装配）+ `components/settings/project-environment-repo-config.tsx` + 原生供给 `crates/cognia-task-workspace/src/service.rs` `apply_provisioning`；门禁 `scripts/gates/check-workspace-attribution.mjs` | Dexie `projects`（叠加层在行上，无新表）；`trustedWorkspaces` 增 3 个非索引字段；SchedulerDB v5 `projectId` | 0144, 0147                                           |
| Python 插件运行时对齐           | 反向 RPC `crates/cognia-plugin-runtime/src/python/`（`host.py` asyncio 宿主 + 专用 stdin 读取线程、`protocol.rs` `host_request` 分类与出站闸、`commands.rs` `plugin_python_host_response`）、渲染端路由 `lib/plugin/python/host-request-router.ts`（**不重查权限**，用的就是 `createGuardedAPI` 包过的 context）、作者面 `plugin-sdk/python/src/cognia/ctx.py`（表从 `_generated_contract.py` 读，回调型方法按 `resourceEffect: returned-disposer` 具名拒绝）、依赖 `crates/cognia-plugin-runtime/src/python/venv.rs`（uv 优先 / 共享桶 / 装前 dry-run 求解）、声明式面板 `components/plugins/plugin-declarative-context-panel.tsx` + `types/plugin/plugin-context-panel.ts`（`kind: "a2ui"                                                                                                                                                                                      | "chat"`）、写入面 `lib/plugin/api/chat-api.ts`、观测 `lib/plugin/observability/plugin-api-spans.ts`         | 无（契约 `catalog.json` 是手写 SSOT → 5 个生成镜像） | 0145 |
| RepoWiki 仓库文档插件           | `plugins/repowiki/`（`plugins/repowiki/repowiki/` 为上游 MIT vendor，算法与测试原样保留；`host.py` 是唯一知道宿主存在的模块——LLM 走 `ctx.agent`、存储走 `ctx.fs.getDataDir()`、枚举走 `ctx.workspace`；`pipeline.py` 单条扫描管线、`panel.py` 纯 A2UI 组件树、`project.py` 稳定 project id + repo map）、入口 `plugins/repowiki/main.py`（7 个 tool + 2 个面板 + `onA2UIAction`）；门禁 `pnpm plugin:repowiki:test`（213 例）+ `plugins/repowiki/manifest.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                               | 无（插件数据目录下两个 SQLite：analyzer 缓存 + RAG 快照）                                                   | 0146                                                 |
| 插件作者边界 (SDK)              | `packages/plugin-sdk/src/api/`（63 个能力模块，以「api 子路径 + capability 名」显式清单发布；根出口只放类型与纯函数——`packages/plugin-sdk/src/index.test.ts` 钉死注册表不得上根）、宿主侧新接口 `lib/plugin/api/`（`agent-turn` 无头角色回合、`session-seed` 带种子新会话、`scheduler-tasks` 用户日程、`workspace-root` 打开的工作目录、`testing` 仅测试用夹具）、`lib/workflow/runtime/trigger-origin.ts`（谁在驱动本轮 → 审批 actorScope）；门禁 `pnpm plugin:author-imports`（`scripts/plugin/author-import-baseline.json` 只允许缩短，现为空）                                                                                                                                                                                                                                                                                                                               | 无（SDK 是宿主类型的再导出面）                                                                              | 0155, 0156                                           |
| 身份平面（人 / 组织 / 成员）    | `types/identity/`（`usr_`/`org_` 词表 + `personStandingFrom` 推导 guest）、`lib/identity/`（`sign-in`/`complete-sign-in` 首登绑定、`user-binding` LocalProfile↔User、`identity-projection` 投影、`external-person` IM 主体收编、`host-person` 宿主归属）、`lib/db/identity.ts`（成员读写 + `listWorkspaceRoster` 派生 guest + `replaceWorkspaceRoster`）、`lib/logto/`（自托管 Logto OIDC + organization）、`src-tauri/src/companion_api/host_identity.rs`（`host_bindings` 三元组）、设备归属判定 `security_store.rs` `OWNER_PREDICATE_SQL` + `lib/devices/grant-capabilities.ts`（`ownerPermits`/`suspended`）；门禁 `scripts/gates/check-identity-vocabulary.mjs`                                                                                                                                                                                                             | Dexie v194（`users`/`orgs`/`orgMemberships`/`workspaceMemberships`/`externalIdentities`）、v196             | 0149, 0054, 0136, 0143                               |
| 协作面（服务端权威）            | `crates/cognia-collab-server/`（`model`/`store`/`api`/`auth`；迁移 0001 身份+Issues、0002 workspaces、0003 plans/runs，全部 ENABLE + FORCE RLS）、共享 `crates/cognia-tenant-auth/`（HMAC grant + RBAC 阶梯 + `set_config('app.tenant_id', $1, true)` 事务级 RLS 夹具 + 冻结线格式向量）、`lib/collab/`（`connection` 服务器在哪、`client` 两段凭据、`sync` 四条拉取、`refresh` 唯一的那次拉取）、镜像 `lib/db/collab-{issue,workspace,plan,run}-mirror{,-types}.ts`、界面 `components/settings/companion/collaboration-card.tsx` + `components/workspace/workspace-{members,activity}.tsx`、来源 `lib/issues/sources/collab-source.ts`；启动由 `lib/issues/boot.ts` 触发                                                                                                                                                                                                        | Dexie v195 / v197 / v198（四张全部可重建的只读缓存；服务端 Postgres 才是权威）                              | 0149, 0132, 0045                                     |
| 浏览器伴侣（Browser Companion） | `packages/companion-client/`（DPoP proof + token 会话 + `cgnb1` 配对载荷 + 线上契约，App/CLI/扩展三方共用）、`types/browser-companion/`（对 package 的再导出）、`lib/browser-companion/`（`build-prompt` 围栏组合器、`limits` 字节闸、`service` 决策、`host-dispatch` 生产接线、`run-status` 状态投影、`appearance` 外观下发）、`lib/db/browser-submissions{,-types}.ts`、`components/settings/companion/browser-companion-card.tsx`、Rust `src-tauri/src/companion_api/extension_origin.rs`（扩展 origin 准入，不放宽 `is_secure_or_loopback`）+ `security_store.rs`（`browser_enrollments` / `browser_devices` / `browser.submit` / `browser.read-own`）+ `api.rs`（browser register 路由 + bound-origin 强制）                                                                                                                                                                | Dexie v199（`browserSubmissions`，仅本机旁注）                                                              | 0154                                                 |
| 引用与结果复用                  | `lib/chat/mentions/`（`entity-sources` 注册表——7 个命名空间 + `shortcut` 触发字符；`entity-cache` 按工作区/会话缓存候选；`message-reference` `@msg:` 与消息区间；`result-reference` + `tool-output-text` `@result:`/`^` 的正文；`selection-freshness` 快照指纹；`backlinks` 反向读）、派生表 `lib/db/chat-result-index.ts` + `lib/db/mention-links.ts`（与 `chat-search-text` **共用同一次消息遍历**）、`components/chat/mention-backlinks-chip.tsx`（chat-header 徽章 + 记录侧面板）、`components/chat/composer-reference-host.tsx` + `lib/chat/composer-reference-request.ts`（⌘K → 聚焦组合器）、`lib/global-search/referenceable.ts`（⌘K 行 → 同一个 `EntityMentionCandidate`）、`lib/chat/save-message-as-memory.ts`（回复 → 待审记忆草稿）                                                                                                                                 | Dexie v204（`chatResultIndex`）/ v205（`mentionLinks`），均为可重建派生数据                                 | 0157                                                 |
| Artifacts / Canvas              | `stores/artifact/artifact-store.ts`（artifact 与 canvas 文档同一个 store）、`lib/artifacts/`（`lib/artifacts/dexie-bridge.ts` 写穿镜像 + 两条账户安全规则、`lib/artifacts/localstorage-migration.ts` 防崩溃搬迁、`lib/artifacts/export/` 真正的 png/pdf、`lib/artifacts/preview-registry.ts` 挂载节点栅格化、`lib/artifacts/tool-part.ts` 唯一转换器、`lib/artifacts/reveal.ts`）、`lib/canvas/dexie-bridge.ts`、`lib/canvas/ai/context-analyzer.ts` 作用域块、`lib/canvas/utils.ts` 大文档降级、`lib/db/artifact-types.ts` + `lib/db/canvas-types.ts`、`components/artifacts/` + `components/canvas/`、Agent 工具面 `lib/claude/artifact-builtin-tools.ts`（8 个工具，骑 `pluginTools` 中继） + `lib/claude/permissions/artifact-tool-rules.ts`（每名登记两遍）、工作流 `lib/workflow/nodes/artifacts/`（`action.artifact.*` / `action.canvas.*` 共 6 个）                      | Dexie v206（`artifacts` + `artifactVersions`）；persist v7（blob 只剩 dock 偏好）                           | 0139, 0158                                           |

### Cross-cutting hooks (reuse, don't reinvent)

- **PII redaction**: `packages/redact/src/index.ts:hasNoLeakingPii` is the gate before any LLM/embed call (encrypted master key in `lib/twin/ingest/redaction-key.ts`). Shared by Twin, Goal, Connector auto-mode (`lib/connectors/ai-loop/safe-send-prompt.ts`), and Agent Team shared-memory.
- **Quiet hours**: `lib/connectors/outbound-runner.isInQuietHours` / `msUntilQuietEnd` exported; reused by `lib/connectors/sla.ts`, `lib/connectors/derive-job-badge.ts`, `components/inbox/quiet-hours-chip.tsx`, and `hooks/pet/use-pet-proactive.ts`.
- **Build-options pipeline**: `lib/claude/build-options.ts:resolveSendOptions` is where A2UI, brief mode, active goal, computer-use tools, twin runtime, and the per-channel A2UI capability prompt converge (incl. the IM computer-use blacklist).
- **A2UI ⇄ IM bridge**: `lib/connectors/a2ui-bridge/*` projects assistant surfaces into platform-native rich content and routes inbound callbacks via `ConnectorBus.dispatchConnectorCallback`. Shared toolkit: `lib/connectors/adapters/_shared/a2ui-mapper.ts`.
- **Static-export caveat**: `app/api/` does not exist in production — anything needing an HTTP server (MCP HTTP, webhook receiver, cron daemon, headless V2 API) lives in Tauri Rust (axum), not Next.js routes.
- **Server-only packages**: vector-DB SDKs + `simple-git` are aliased to `lib/browser-stubs/empty.js` in `next.config.ts`. Add new server-only deps to **both** `SERVER_ONLY_PACKAGES` and `serverExternalPackages`; Node-only built-ins go in `NODE_ONLY_MODULES`. Wrong setup blows up the mobile bundle.

## Testing Standards

- **Co-located**: `xxx.test.ts(x)` next to source. No `__tests__/` or `tests/` directories. Enforced by `pnpm audit:colocated-tests`; the 452 pre-existing gaps are recorded in `scripts/gates/colocated-test-baseline.json` and that list may only shrink.
- **Rust**: in-file `#[cfg(test)] mod tests { ... }`; integration tests in `src-tauri/tests/` allowed
- **Excluded**: `components/ui/` (shadcn), `components/ai-elements/` (vendored), and `web/components/ui/` (the marketing workspace's own shadcn copy) — don't add tests there. `web/app/` and `web/content/` are also exempt: thin route glue, and data whose en/zh parity `tsc` already proves.
- **Sidecar**: `pnpm sidecar:test` (uses `node --test`, not Jest)

Which runner owns what, and where each runs, is documented in [`CI_CD.md`](./CI_CD.md).

## Commit Hooks

Husky is installed via the root `prepare` script — `pnpm install` once and the hooks are live.

- **`pre-commit`** → `lint-staged` (runs `eslint --fix` + `prettier --write` on staged files)
- **`commit-msg`** → `commitlint` (`@commitlint/config-conventional`) — Conventional Commits enforced

Never bypass with `--no-verify`. If a hook fails, fix the root cause, re-stage, and create a **new** commit (the failed commit was never created).

## Versioning & Release (Changesets)

The app is versioned as **one unit**: the root `package.json` `version` is the single source of truth, and [Changesets](https://github.com/changesets/changesets) manages both the version bump and `CHANGELOG.md`.

- **During development** — for any user-facing change, run `pnpm changeset` and select the **`cognia-next`** package (Working Rule 6). Each run writes a `.changeset/*.md` entry that you commit with the code; they accumulate until the next release.
- **At release time** — run `pnpm release:version`. This runs `changeset version` (consumes the pending `.changeset/*.md`, bumps the root version, prepends the aggregated entries to `CHANGELOG.md`) and then `pnpm version:sync`, which propagates the new version to every app artifact (`src-tauri/tauri.conf.json`, the three `Cargo.toml`s, `cli/`, `sidecar/`, `sidecar/vscode-ext-host/`, `mobile/`, `docs/`). Commit the result and tag it.
- **Scope** — only the root `cognia-next` is Changesets-managed. `docs`, `mobile`, and every `@cognia/*` workspace package are in `ignore` (`.changeset/config.json`) and never versioned or published this way. The root is listed in `pnpm-workspace.yaml` **only** so Changesets can see it; pnpm still excludes it from recursive commands by default.
- **No CI/publish step** — the flow is local-only and does not publish to npm; `access` is `restricted`. Do not add a `changeset publish` step or a release GitHub Action without confirming with the user first.
- `pnpm changeset:status` shows what would be released.

## Critical Notes

- **pnpm only** — install from repo root
- **Do not remove `output: "export"` in `next.config.ts`** — Tauri and Capacitor builds both consume `out/`. `docs/next.config.ts` is also a static export (`docs/out/`); keep the two configs separate
- **Native vector store**: sqlite-vec at `<app_data>/cognia/vectors.sqlite`. Web mode hides the native option and forces cloud
- **Dexie schema is ONE version, not a chain**: `lib/db/schema.ts` declares the whole store set once as `CURRENT_SCHEMA` at `CURRENT_SCHEMA_VERSION` (currently **v220**). There is no historical `version(N)` chain and no `upgrade()` hook. To change it: edit `CURRENT_SCHEMA`, **bump `CURRENT_SCHEMA_VERSION`** (IndexedDB only upgrades when the number rises, so an un-bumped edit is silently ignored on every existing database), add the table to `CORE_TABLE_NAMES` in `lib/data-governance/table-catalog.ts`, and run `pnpm db:governance:write`. To drop a table set its value to `null` — never delete the key. A database whose `storageLayout` marker does not match is refused at boot and the user is routed to an explicit reset; existing local data is not migrated
- **Rust toolchain**: 1.77.2+
- shadcn/ui: "new-york" style, RSC mode
