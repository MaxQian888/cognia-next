// Re-export shim: canonical source moved to @cognia/provider-routing (Stage 4).
export {
  BUILT_IN_ROUTING_SELECTORS,
  adaptiveSelector,
  balancedSelector,
  costSelector,
  leastBusySelector,
  makeTelemetrySnapshot,
  qualitySelector,
  speedSelector,
} from "@cognia/provider-routing/strategies/built-in"
