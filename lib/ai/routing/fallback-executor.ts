// Re-export shim: canonical source moved to @cognia/provider-routing (Stage 4).
export { executeFallbackChain } from "@cognia/provider-routing/fallback-executor"
export type {
  FallbackConfig,
  FallbackExecutorDeps,
  ProviderAttemptResult,
} from "@cognia/provider-routing/fallback-executor"
