// Re-export shim: canonical source moved to @cognia/provider-embedding (Stage 3).
export { embedTokens, scoreLateInteraction } from "@cognia/provider-embedding/late-interaction"
export type {
  LateInteractionConfig,
  TokenEmbedding,
} from "@cognia/provider-embedding/late-interaction"
