// Re-export shim: canonical source moved to @cognia/provider-core (Stage 2).
export {
  COMMON_CONNECTION_PARAMETERS,
  COMMON_INFERENCE_PARAMETERS,
  PROVIDER_SCHEMAS,
  getSchemaForProvider,
} from "@cognia/provider-core/providers/provider-parameter-schemas"
