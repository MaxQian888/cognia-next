// Re-export shim: canonical source moved to @cognia/provider-core (Stage 2).
export {
  currentStateValue,
  recordFailure,
  recordSuccess,
} from "@cognia/provider-core/providers/circuit-breaker-machine"
export type { RecordFailureOptions } from "@cognia/provider-core/providers/circuit-breaker-machine"
