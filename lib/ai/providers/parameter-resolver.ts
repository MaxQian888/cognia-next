// Re-export shim: canonical source moved to @cognia/provider-core (Stage 2).
export {
  resolveInferenceParams,
  resolveProviderSpecificParams,
  setNestedValue,
  shouldShowParameter,
} from "@cognia/provider-core/providers/parameter-resolver"
