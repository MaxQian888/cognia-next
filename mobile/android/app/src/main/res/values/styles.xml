<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Launch theme — Android 12+ SplashScreen API (androidx.core:core-splashscreen).
         windowSplashScreenBackground: fixed deep-navy (@color/splash_background,
           identical in light + night) so the launch surface never flashes white.
         windowSplashScreenAnimatedIcon: the circular cognia icon, centered &
           sized by the system.
         postSplashScreenTheme: the normal app theme applied once the splash exits.
         Held until CompanionBootProvider calls hideSplash() after React's first
         paint (capacitor.config launchAutoHide=false). This replaces the old
         `android:background="@drawable/splash_themed"` layer-list, which the
         Android 12+ system splash ignores (it produced a white card on black). -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>