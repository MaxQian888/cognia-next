node_modules/

# Platform directories — populated by `cap add ios` / `cap add android`
# in M3.2 / M3.3. Once a platform is committed, remove the matching line.
ios/

# Android build outputs — keep the source tree, drop generated bytecode.
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/captures/
android/local.properties
android/app/release/
android/app/debug/
