export const PLUGIN_ID = "strix-security"

/** Local id of the rail view container (namespaced `<pluginId>:security`). */
export const CONTAINER_ID = "security"

/** Local id of the React panel view. */
export const VIEW_ID = "strix-panel"

/** Slash command that opens the panel. */
export const OPEN_COMMAND_ID = `${PLUGIN_ID}.open`
