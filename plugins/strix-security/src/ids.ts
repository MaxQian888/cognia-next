export const PLUGIN_ID = "strix-security"

/** Local id of the Context Workbench panel (namespaced `<pluginId>:security`). */
export const PANEL_ID = "security"

/** Namespaced panel id, as the workbench registry stores it. */
export const PANEL_FULL_ID = `${PLUGIN_ID}:${PANEL_ID}`

/** Slash command that reveals the panel. */
export const OPEN_COMMAND_ID = `${PLUGIN_ID}.open`
