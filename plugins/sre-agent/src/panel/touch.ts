/**
 * Target sizes for the panel's controls.
 *
 * The same panel runs in the desktop workbench and in a 375px touch WebView.
 * Below `sm` every control is at least 36px tall (the touch floor); from `sm`
 * up it keeps the compact density a mouse-driven side panel wants.
 */
export const TOUCH_BUTTON = "h-9 px-3 text-xs sm:h-7 sm:px-2"
export const TOUCH_ICON_BUTTON = "size-9 shrink-0 sm:size-7"
