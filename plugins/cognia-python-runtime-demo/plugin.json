{
  "id": "cognia-python-runtime-demo",
  "name": "Python Runtime Contributions Demo",
  "nameKey": "manifest.name",
  "version": "0.1.0",
  "description": "First-party reference for python-backed module-bridge contributions: an OCR provider, an AI provider, a decision provider, a workspace backend, and a flag-gated connector — all executing in the Python subprocess.",
  "descriptionKey": "manifest.description",
  "type": "python",
  "capabilities": [
    "python",
    "media",
    "ai-provider",
    "decision-provider",
    "workspace-backend",
    "connectors"
  ],
  "pythonMain": "main.py",
  "author": "cognia-next",
  "license": "MIT",
  "minAppVersion": "0.1.0",
  "engines": {
    "cognia": ">=0.1.0",
    "python": ">=3.9"
  },
  "permissions": ["python:execute", "decisions:provide"],
  "providesServices": {
    "workspace.backend": "1.0.0"
  },
  "ocrProviders": [
    {
      "id": "echo-ocr",
      "label": "Echo OCR (Python)",
      "description": "Returns the input path as recognized text — proves the seam, not the OCR."
    }
  ],
  "aiProviders": [
    {
      "id": "echo-llm",
      "label": "Echo LLM (Python)",
      "kind": "llm",
      "models": ["echo-small"]
    }
  ],
  "decisionProviders": [
    {
      "id": "echo-decision",
      "label": "Echo decisions (Python)",
      "description": "Answers every typed question with a fixed, uncalibrated verdict — proves the seam, not the judgment."
    }
  ],
  "workspaceBackends": [
    {
      "id": "memory-workspace",
      "label": "In-memory workspace (Python)",
      "description": "Records clone/commit/remove calls in the subprocess."
    }
  ],
  "connectors": [
    {
      "type": "echo-chat",
      "factory": "EchoConnector",
      "configSchema": {
        "type": "object"
      },
      "transportModes": ["longpoll"]
    }
  ],
  "runtimeCompatibility": {
    "tauri": {
      "availability": "supported"
    },
    "browser": {
      "availability": "blocked",
      "reason": "Python plugins run in the desktop subprocess host."
    },
    "mobile": {
      "availability": "blocked",
      "reason": "Python plugins run in the desktop subprocess host — unavailable on mobile."
    }
  },
  "i18n": {
    "locales": {
      "en": {
        "manifest.name": "Python Runtime Contributions Demo",
        "manifest.description": "First-party reference for python-backed module-bridge contributions: an OCR provider, an AI provider, a decision provider, a workspace backend, and a flag-gated connector — all executing in the Python subprocess."
      },
      "zh-CN": {
        "manifest.name": "Python 运行时贡献示例",
        "manifest.description": "Python 驱动的模块桥贡献的官方参考：OCR 提供方、AI 提供方、决策提供方、工作区后端和一个受开关控制的连接器——全部在 Python 子进程中运行。"
      }
    }
  }
}
