# 2026-09-23 reported defects: repair and verification

This report covers the user's ten OPEN defects and six UX observations. The checkout contained extensive unrelated and overlapping edits before this task. Existing changes were preserved; no commit, staging, reset, or push was performed.

## Ten reported defects

| Finding                                                             | Result and evidence                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Inbox `TransactionInactiveError` collapses the entire page          | Reused `InboxErrorBoundary` around the sidebar, conversation list, notices, and detail on desktop and mobile. A failed pane offers Retry while sibling panes remain usable. Tests inject the database error and verify containment/recovery. This limits the failure radius; it does not claim to repair every encrypted IndexedDB transaction cause.                                                                                   |
| Tauri `listeners[eventId].handlerId` unhandled rejection            | Patched verified raw Tauri disposer paths to use the existing `safeUnlisten`, including island/native agent events, CLI/companion/sync/plugin bridges, automation, recorder/OCR/media/OneBot progress, and tray/RPC sources. Regression tests exercise rejecting unregister promises and early disposal. The original runtime stack was not supplied, so its exact caller remains unproven.                                             |
| `desktop-host-state` calls unavailable `host_feature_manifest`      | Local desktop no longer installs remote mirror synchronization against raw Tauri IPC. Synchronization subscribes to and captures an active remote transport; local desktop remains its own database authority. Local startup and remote transport changes are tested.                                                                                                                                                                   |
| Gateway rejects snapshot for `alias fast: no executable candidates` | Valid but temporarily unavailable aliases no longer reject the entire snapshot, which previously also prevented unrelated routes and credential revocation from updating. Request-time resolution still rejects an unavailable route. Structural/policy validation remains.                                                                                                                                                             |
| `secrets.get PluginGatewayError` lacks origin and cause             | Added final-failure diagnostics containing plugin ID, API, typed error code, request ID, and attempt. Payloads, secret values, and opaque host error details are excluded. **The original plugin and failure cause still require a redacted runtime log.**                                                                                                                                                                              |
| Pi/commandcode incorrectly show `No API key`                        | The composer warning for built-in model credentials is now limited to the built-in runtime. External runtimes use their own credential/configuration state.                                                                                                                                                                                                                                                                             |
| Locked cold boot permanently misses configured capabilities         | Startup probing waits for the unlocked account and reruns after unlock/account changes. Results from an old account or a newly locked account are discarded.                                                                                                                                                                                                                                                                            |
| Disabled adapter presence task keeps running/failing                | The runner skips disabled adapters before bus access. Startup, disable, and re-enable reconciliation read current persisted presence settings. Automatic pauses carry a durable ownership tag, allowing restart recovery without resuming manually paused tasks. Schedule timers/retries are retired through the existing scheduler.                                                                                                    |
| Pi handshake failure leaves workspace lease                         | Fixed controller ordering: clearing a session error also sets it idle, so it must happen **before** entering streaming. Previously the tracker observed idle before lease acquisition and missed the later terminal edge. A controller/tracker integration test acquires the lease, fails before streaming output, settles it as failed, then reacquires it on the next send. Native workspace and Pi dispatch are mocked in this test. |
| Keychain cancellation cached permanently in `OnceLock`              | Current checkout already contains success-only caching, retryable failed initialization, and a cold-boot Retry UI wired to the native command. Preserved those changes and verified the existing Rust and UI regressions. No new Rust Keychain implementation was added.                                                                                                                                                                |

## Six UX observations

| Finding                                                        | Result                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `/logs` workspace preparation lacks feedback                   | Current `PageLoading` → `BootScreen` already shows milestone progress, capability readiness, elapsed waiting feedback, and reload escalation. Wiring and tests verified. `/logs` rendered successfully in the browser; no claim that native cold boot now takes less time.                                               |
| Cold route `router.push` appears silent                        | Shared shell navigation now tracks the actual React transition. Sidebar and GuildRail show pending spinners/live loading feedback for pinned routes and More-menu navigation until the route commits. Browser verification observed Loading during Logs → Scheduler and successful arrival.                              |
| Agent Runs shows `Overdue` for every historical run            | Replaced future-deadline formatting with `next-intl` relative time in the run list, detail timestamps, and delegation review history.                                                                                                                                                                                    |
| Session cost chip and popup disagree                           | External reported usage is persisted through the existing usage ledger. Chip and popup use the same known cumulative totals; legacy/partial history preserves known counters without double-counting and marks model details as partial. Unknown complete turn counts/durations/speeds are not invented.                 |
| Internal dotfiles dominate `@` file suggestions                | Workspace traversal visits visible siblings first and ranking deprioritizes dot-paths unless explicitly queried. Explicit `.pi/tasks/...` searches still work; gitignore behavior remains.                                                                                                                               |
| Duplicate demo-heartbeat names and noisy maintenance reminders | Same-name scheduler rows show type and stable source ID without renaming/deleting tasks. Provider diagnostics defaults and legacy unspecified settings disable due reminders; explicit user opt-in and failure preferences are preserved. Presence/system maintenance reminders also default quiet in the reminder hook. |

## Verification boundaries

- Focused Jest regressions cover boot/transport switching, Inbox isolation, teardown races, adapter schedule lifecycle, scheduler identity/reminders, elapsed-time UI, credentials, usage accounting, and the Pi lease lifecycle.
- Rust checks: `cognia-secrets` secret-store tests (23 passed), gateway snapshot tests (8 passed), `cognia-files` search tests (8 passed), and `cargo clippy -p cognia-files --lib -- -D warnings` passed.
- Changed TypeScript/TSX files received focused ESLint checks. Translation sources were built; freshness, ICU validation, and `lint:i18n` passed.
- Browser checks used an isolated agent-browser session against the existing development server: Agent Runs, Logs, the Inbox pairing gate, and a real pending shell transition to Scheduler. The browser was not paired with a native host.
- Full-workspace typecheck is **not green**. The first filtered invocation exhausted Node's default heap; a direct package-script retry used its configured 16 GiB heap and exposed shared-checkout type errors. Errors introduced in new tests were corrected; unrelated prototype/workflow/Router Fusion diagnostics are not claimed fixed.
- This is not a production build, live Keychain cancellation test, authenticated encrypted Inbox test, or live native Pi handshake acceptance. Those claims require a running rebuilt native host and the original account/runtime setup.

The original `secrets.get` failure remains the sole report item without an identified underlying cause. The original unlisten stack would also establish whether the observed caller is among the repaired paths.
