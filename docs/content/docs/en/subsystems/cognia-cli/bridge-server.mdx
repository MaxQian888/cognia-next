---
title: Bridge server
description: The desktop side of the shared loopback bridge — axum on an ephemeral 127.0.0.1 port, per-launch authentication, 12 routes for both local CLIs, plugin operations, session events, and renderer-backed twin/team requests.
---

# Bridge server — `src-tauri/src/cli_bridge`

<TLDR>
  `cli_bridge::init()` (`mod.rs:158-183`) runs in a spawned task inside Tauri's `setup()` hook:
  generate a 32-byte / 64-hex dev token, `server::spawn()` an axum router bound to
  **`127.0.0.1:0`** (OS-assigned ephemeral port), write `cli-endpoint.json` (`0o600` on Unix),
  and stash `RunningBridge {bound_port, shutdown}` in managed state for graceful shutdown on app
  exit. Every request passes one middleware:
  non-loopback peers get 403 `remote_blocked`; a wrong/missing `X-Cognia-Dev-Token` gets 401
  `bad_dev_token` after a constant-time comparison. Plugin handlers mutate `PluginRuntimeState`
  directly; plugin/session handlers emit events, while twin/team handlers use a pending renderer
  request/response bridge. This page focuses on the plugin-author CLI subset; the full shared
  topology is documented in [CLI ↔ App Bridge](../cli-app-bridge).
</TLDR>

## Lifecycle

```mermaid
flowchart LR
  SETUP[tauri setup hook<br/>lib.rs:905-1238] --> INIT[cli_bridge::init<br/>mod.rs:158]
  INIT --> TOK[generate_dev_token<br/>32 rand bytes → 64 hex<br/>mod.rs:115-122]
  INIT --> SPAWN[server::spawn<br/>bind 127.0.0.1:0<br/>server.rs:28]
  SPAWN --> EPF[write_endpoint_file<br/>baseUrl + devToken · 0o600<br/>mod.rs:128-150]
  INIT --> STATE[CliBridgeServerState<br/>RunningBridge - port, watch::Sender]
  EXIT[RunEvent::Exit<br/>lib.rs:1249] --> SHUT[shutdown → watch signal<br/>graceful drain<br/>mod.rs:345-350]
```

Startup is **best-effort**: an endpoint-file write failure is logged, not fatal, and a bridge
start failure logs `cli_bridge auto-start failed` without blocking the app. The token rotates
every launch; the file is rewritten each boot, so a stale file from a crashed session simply
carries a dead port + token. The current native startup block has no explicit mobile compile-time
guard; renderer listeners and sync controls are desktop/Tauri-gated.

## Auth middleware

```rust
// src-tauri/src/cli_bridge/server.rs:88-115 (abridged)
async fn auth_middleware(State(state), ConnectInfo(addr), request, next) -> Response {
    if !is_loopback(&addr) {                       // mod.rs:189 — 127.0.0.1 or ::1
        return error_response(StatusCode::FORBIDDEN, "remote_blocked",
            "the cli bridge accepts loopback (127.0.0.1) connections only");
    }
    let header = request.headers().get("X-Cognia-Dev-Token")…;
    if !constant_time_eq(header.as_bytes(), state.dev_token.as_bytes()) {  // server.rs:127
        return error_response(StatusCode::UNAUTHORIZED, "bad_dev_token",
            "X-Cognia-Dev-Token header missing or incorrect");
    }
    next.run(request).await
}
```

`constant_time_eq` (`server.rs:127-136`) XOR-accumulates over all bytes — no early exit, no
timing oracle. The middleware wraps **all** routes, including `health`, via
`from_fn_with_state` (`server.rs:80`), under a 4 MiB `RequestBodyLimitLayer` (`server.rs:81`).

## Routes

```rust
// src-tauri/src/cli_bridge/server.rs:57-80
Router::new()
    .route("/api/v1/dev/health",            get(handlers::health))
    .route("/api/v1/dev/plugins/installed", get(handlers::list_installed))
    .route("/api/v1/dev/plugins/install",   post(handlers::install))
    .route("/api/v1/dev/plugins/install-directory", post(handlers::install_directory))
    .route("/api/v1/dev/plugins/uninstall", post(handlers::uninstall))
    .route("/api/v1/dev/plugins/reload",    post(handlers::reload))
    .route("/api/v1/dev/acp/token",         post(handlers::acp_token))
    .route("/api/v1/dev/sessions/handoff",  post(handlers::handoff))
    .route("/api/v1/dev/twin/context",      post(handlers::twin_context))
    .route("/api/v1/dev/teams/list",        post(handlers::teams_list))
    .route("/api/v1/dev/teams/run",         post(handlers::teams_run))
    .route("/api/v1/dev/teams/run-status",  post(handlers::teams_run_status))
```

All responses share the envelope `{"ok": true, "pluginId"?, "warnings"?: []}` /
`{"ok": false, "error", "code"?}`. Failures from handlers are 422; missing required fields in
`reload` are 400.

| Handler | Core logic | Lines |
| --- | --- | --- |
| `health` | `{"ok": true}` liveness probe | `handlers.rs:107` |
| `list_installed` | read-locked snapshot of `PluginRuntimeState.plugins`, projected to `pluginId / version / status / installPath` only — never `runtime_state`, so secrets can't leak through the bridge | `handlers.rs:136-155` |
| `install` | `install_inner` → emit `cli-bridge:plugin-installed` | `handlers.rs:157-190` |
| `install_directory` | `install_from_directory_inner` → copy an unpacked plugin directory and emit `cli-bridge:plugin-installed` | `handlers.rs:192-221` |
| `uninstall` | `uninstall_inner` → emit `cli-bridge:plugin-uninstalled` | `handlers.rs:223-252` |
| `reload` | three modes (below) → emit hot-reload events | `handlers.rs:254-364` |
| `handoff` | validate `sessionId`, build a transcript payload, then emit `cli-bridge:session-handoff` for renderer import/open | `handlers.rs:380-397` |

### install_inner (`handlers.rs:403-429`)

Validate the bundle path (must be absolute and exist) → read the zip → extract the manifest for
`plugin_id` → wipe any previous version of the plugin dir → extract → `register_installed_plugin`
into `PluginRuntimeState`. Extraction defends against **zip-slip**: entries resolving outside
the target directory are skipped (test `extract_zip_into_skips_path_traversal_entries`,
`handlers.rs:741-766`).

### install_from_directory_inner

Validate the source path (must be absolute, existing, and a directory containing `plugin.json`) →
read the manifest for `plugin_id` → wipe any previous version of that plugin dir → recursively
copy files into the host plugin directory → `register_installed_plugin`. Symlinks are deliberately
skipped so a load-unpacked workflow cannot copy outside the selected source tree.

### reload's three modes

| Mode | Trigger | Behavior |
| --- | --- | --- |
| A — reinstall | body has `bundle_path` | full `install_inner`, then hot-reload events with `via: "install"` — this is what `cognia plugin dev` uses |
| B — reinstall unpacked | body has `source_dir` | full `install_from_directory_inner`, then hot-reload events with `via: "install-directory"` |
| C — hot-reload only | body has `plugin_id` only | no file work; events with `via: "reload"` ask the renderer to re-read the artifact already on disk |

Each success emits **two** events: the per-plugin channel `plugin-hot-reload:<plugin_id>` with
`{"source": "cli-bridge"}` (legacy subscribers that already know their id) and the global
`plugin-hot-reload` with `{"plugin_id", "source": "cli-bridge", "via"}` (the centralized
hot-reload history store).

## State & concurrency

```rust
// src-tauri/src/cli_bridge/mod.rs:62-71
pub struct CliBridgeState { dev_token: String, app_handle: AppHandle }   // Arc<…> = SharedState
```

The token is immutable per launch — no lock. Server lifecycle lives in
`CliBridgeServerState { inner: parking_lot::Mutex<Option<RunningBridge>> }` (`mod.rs:75-110`),
held only for insert/take. Plugin mutations go through `PluginRuntimeState`'s per-field
`Arc<RwLock<HashMap<…>>>` (`plugin_api/mod.rs:91-108`) — requests are independent; there is no
single-flight guard, matching the single-developer-loopback threat model.

## Renderer round-trip

Plugin and session notifications are **fire-and-forget**. Twin context and the three team routes
instead allocate a Rust pending oneshot, emit `cli-bridge://renderer-request`, and resolve through
the `cli_bridge_renderer_response` Tauri command. Rust expires these requests after 30 seconds;
the agent CLI aborts its fetch after 10 seconds. The renderer's event hooks handle the asynchronous
plugin/session channels:

| Event | Payload | Renderer action |
| --- | --- | --- |
| `cli-bridge:plugin-installed` | `{plugin_id}` | refresh plugin manager list |
| `cli-bridge:plugin-uninstalled` | `{plugin_id, purge_data}` | drop from list; `purge_data: true` triggers the Dexie wipe (TS owns plugin tables) |
| `plugin-hot-reload` (+ per-id channel) | `{plugin_id, source, via}` | unload + reload the plugin artifact |
| `cli-bridge:session-handoff` | `{sessionId, title, messages, meta}` | import the standalone agent transcript and open it in the renderer |

## Adjacent Tauri commands

Registered alongside the HTTP bridge (`lib.rs:435-439`): `cli_bridge_status` (renderer status
probe), `plugin_install_from_directory` + `preview_local_manifest` (the DevTools "load unpacked"
path, now sharing the same directory-install helper exposed to the standalone CLI bridge —
`copy_dir_recursive` lives at
`handlers.rs:569-588` and is tested at `handlers.rs:719-738`), `detect::detect_binary`, and `download::download_cognia_cli`
([next page](./detection-and-managed-install)).

42 focused in-file tests cover token shape/uniqueness (`mod.rs:355-367`), loopback detection
(`:369-377`), endpoint-file round-trips (`:379-420`), `constant_time_eq` (`server.rs:143-150`),
manifest-from-zip extraction and rejection paths, and the zip-slip defense
(`handlers.rs:638-766`).
