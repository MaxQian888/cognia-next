# GitHub Devin Bot implementation and verification

Date: 2026-09-12; updated: 2026-09-13

Target repository: `NJUPT-SAST/sast-approval-next` (editable installation setting).

## Delivery status

The installable plugin and trial services are running. The user authorized unattended execution and automatic PR/review publication for `NJUPT-SAST/sast-approval-next`, using the bound MaxQian888 account and `swe-2-max`. Monitoring is enabled. Real execution completed issue #25 and automatically published [PR #26](https://github.com/NJUPT-SAST/sast-approval-next/pull/26) without a human approval click; the published file matches the retained snapshot and its 39 tests pass independently. All 13 currently returned GitHub check runs are successful (including a second macOS build); the earlier snapshot had 12 successful checks. The earlier user-approved PR #24 has passed all returned CI checks.

This remains **short of complete production acceptance** of every original plan item. Live automatic third-party review verdicts and CI repair publication have not yet been exercised against an actual triggering result. Manual command approval still has a four-minute live deadline and consumes execution time; unattended execution avoids those prompts. Repository-wide gates retain the unrelated failures listed below. The chronological trial notes preserve earlier setup and failure evidence; this current status supersedes their activation/publication status.

Package: `plugins/github-devin-bot/github-devin-bot.zip` (SHA-256 `e8fc1ff9ce1347d4285a3bbae5f58eee75d2dfbc0f575aec1442f557362f254c`). Build/install details are in the plugin README. The package depends on the updated host APIs and the updated `github-delivery` plugin in this checkout; installing it into an older Cognia binary does not install those host changes.

## Implemented path

1. The builtin plugin catalog and distribution loader discover `github-devin-bot`. Its Bot declaration supplies the repository/model configuration, credential slot, manual backfill form, polling and GitHub event triggers, and execution policy.
2. Existing Bot installation/detail screens reuse `AdapterForm`, credential selectors, run/delivery lists, and trigger controls. Paired lifecycle commands and host console reads use the existing authenticated control bridge. The first arming stores an activation watermark; a prior manual scan cannot move it backward. Backfill requires explicit item numbers.
3. `ctx.bots` reads owned installation state, records synchronization health, enqueues stable work identities into the existing delivery table, and cancels obsolete resource work. Optional structured trigger conditions filter repository, branch, labels, actor, draft state, and CI conclusion before model dispatch.
4. Run-bound integration calls resolve the GitHub account on the host. Ownership, plugin dependency, installation binding, repository scope, and action allowlists are checked. Existing account-based calls remain owner-scoped. GitHub Delivery supplies paginated reads, checks, workflow jobs/logs, and lifecycle normalization.
5. Shared workspace/Git APIs allocate a checkout per Bot run. Headless discovers its admitted physical workspace root through `fs_workspace_roots` and uses the existing confined filesystem/Git operations; paired clients retain their opaque-path restrictions. Shared external-agent management dispatches an isolated Devin ACP process using an explicitly confirmed SWE-2 model. Host checkpoints record the workspace, session, dispatch identity, result, and immutable publication snapshot.
6. The existing decision interrupt holds the exact patch, test report, model/session, target SHA, and review/PR text. Run Detail uses the existing source-control diff viewer and authoritative host run-detail reads for paired clients. Parked approvals release the repository execution slot and retain their original seven-day deadline.
7. Publication is host-brokered. It revalidates approval, workspace snapshot, target revision, generated branch identity, and remote state. Git objects and publication identities are checkpointed; uncertain branch/PR results are reconciled before another write. There is no merge action.
8. Disabled installations stop new dispatch and abort active execution. Closed/superseded items cancel obsolete deliveries. Blocked results retain their snapshot and report for inspection instead of silently succeeding.

No separate scheduler, queue database, approval store, Bot dashboard, or Devin launcher was introduced.

## Mature Bot comparison

The following official documentation was checked on 2026-09-12. The implementation choices in the last column are Cognia design decisions, not claims of equivalent product coverage.

| Product                                                                                   | Documented pattern                                                                                                                                                                          | Applied here                                                                                                                                                                      |
| ----------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Probot](https://probot.github.io/docs/)                                                  | GitHub Apps combine granular permissions with webhook delivery. Its [fixture receiver](https://github.com/probot/probot/blob/master/docs/simulating-webhooks.md) supports event simulation. | Use the existing integration account and verified webhook ingress; normalize fixture and polling events through the same handler. Do not add an independent credential store.     |
| [Renovate](https://docs.renovatebot.com/configuration-options/)                           | Repository-scoped branch/PR limits and optional dashboard approval regulate update creation. The dashboard exposes pending, open, closed, and failed work.                                  | Bound repository execution; shared run/approval lists; distinguish deferred work from failures. Cognia approval additionally binds concrete patch content and publication intent. |
| [Mergify](https://docs.mergify.com/configuration/conditions/)                             | Rules evaluate PR attributes such as branches, labels, reviews, and CI. Check names can be qualified by publishing GitHub App to avoid ambiguity.                                           | Structured pre-dispatch conditions, explicit match/skip reasons, and current-revision CI checks. This plugin deliberately provides no merge action.                               |
| [CodeRabbit](https://docs.coderabbit.ai/configuration/auto-review)                        | Non-draft automatic reviews, incremental review after pushes, author exclusions, and optional auto-pause reduce repeated work.                                                              | Review only new heads, remember the last published review revision, exclude drafts/self-generated work, and preserve explicit backfill.                                           |
| [Devin Review and Auto-Fix](https://docs.devin.ai/use-cases/gallery/devin-review-autofix) | Repository/user enrollment triggers reviews on open, push, and ready-for-review events. Its documentation warns that responding to all Bot comments can create loops.                       | Stable revision identities and host provenance suppress recursion. Repairs are approved separate PRs targeting the original branch, rather than direct unsolicited edits to it.   |

This comparison supports keeping provider events, execution, approvals, and publication separate in the existing runtime. It does not justify broadening account access or treating model-reported tests as independently verified results.

## Verification evidence

The following evidence has distinct boundaries; fixture tests do not establish a running monitor or public publication.

| Check                                                  | Observed result                                                                                                                          |
| ------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Core Bot, UI, DB, and API focused test run             | 64 suites / 819 tests passed before final audit fixes; subsequent focused regressions were rerun                                         |
| Queue fairness, recovery, shutdown, and approval tests | Latest core runtime run: 4 suites / 121 tests passed; all four covered files exceed 90% lines, branches, and functions                   |
| New root Bot APIs/ownership/conditions coverage        | 18 tests passed; 99.46% lines, 96.73% branches, 100% functions in the scoped run                                                         |
| Plugin monitor/execution fixture coverage              | Final 70-test run: 99.9% lines, 96.1% branches, 100% functions, including external check failures and uncertain recovery                 |
| External agent/workspace APIs                          | New core modules reached at least 90% lines/branches/functions in scoped coverage; isolation and recovery tests passed                   |
| Shared integration/contract checks                     | Focused integration, binding, validation, bridge, run-control, and run-detail tests passed; final publication audit run: 44 tests passed |
| Run Detail and approval controls                       | 45 tests passed for immutable result inspection and exact inspected-run approval controls                                                |
| Rust launcher                                          | 6 tests passed; launcher binary built successfully                                                                                       |
| Rust external sandbox and spawn policy                 | 21 sandbox tests and 26 spawn-policy tests passed                                                                                        |
| Rust Devin MCP config                                  | 7 tests passed                                                                                                                           |
| Workspace file metadata                                | 24 TS tests and 2 Rust tests passed                                                                                                      |
| Contract/author declarations                           | Contract freshness and 80 bundled author declarations passed                                                                             |
| English/Chinese localization                           | Split-source generation, freshness, and i18n lint passed                                                                                 |
| Static-export import audit                             | 8,619 TypeScript files audited; no Node-only import violations                                                                           |

The final paired lifecycle/UI/bridge verification covered 324 tests across the expanded focused suites, including mirror-write denial, host-switch invalidation, scope validation, and explicit read-error states. The new lifecycle host boundary reached 100% lines/functions and 97.26% branches in its focused coverage run (`/tmp/github-bot-lifecycle-host-coverage.log`). The new paired-host read hook passed 17 tests with 100% statements/lines/branches/functions, covering stale responses, pairing changes, malformed payloads, recovery, and unmount cleanup.

### Real Devin ACP smoke

The existing harness ran with an isolated fixture and the task-built launcher:

```sh
COGNIA_DEVIN_BOT_ISOLATION_SMOKE=1 \
COGNIA_EXTERNAL_AGENT_LAUNCHER=target/debug/cognia-external-agent-launcher \
node scripts/smoke/build-and-run-smoke.mjs --devin-acp
```

Passed: authenticated `swe-2-medium`, native fixture file reads/writes, separate concurrent MCP configurations, sibling session close, session listing/loading/reconnection, follow-up continuity, and cancellation acknowledgement. A synthetic credential fixture was inaccessible outside the allowed workspace; GitHub/SSH credentials were removed from the child environment. No public GitHub write was part of this smoke.

Local evidence log: `/tmp/github-bot-devin-isolation-smoke.log` (temporary, not committed).

### GitHub and UI evidence

Read-only GitHub API inspection confirmed that the target is public, unarchived, and uses `master`. The current `gh` identity has read/write access. There were 19 open issue/PR items and recent successful workflows at inspection time. This **does not** mean Cognia has a configured integration account: the isolated Cognia fixture has no integration account, and its new Bot installation remains `needs_setup`.

A real browser loaded the reused Bot page and installation surface. It correctly displayed that the standalone browser cannot run Bots without a paired host. Real Headless RPC acceptance subsequently verified plugin activation, executable catalog discovery, installation, duplicate-request reconciliation, model configuration round trips, disable/re-enable, and `needs_setup` with automatic triggers unarmed. The complete visual paired installation, approval, retry, and disable flow has **not** been driven end to end in a running desktop/paired application. Temporary screenshots: `/tmp/github-bot-ui.png`, `/tmp/github-bot-setup-blocked.png`.

The isolated fixture installation is `boti_0c5d9be6-2f29-41bc-9732-65631b8a652d`, definition `github-devin-bot:repository-monitor`. Its saved configuration is the target repository, `swe-2-medium`, 30 minutes, and two repair attempts. The `github` slot is unbound. Poll and GitHub event triggers remain unarmed; enabled manual entrypoints do not represent active monitoring. This is a development-host installation, not an installation into an already running production desktop profile. After acceptance, the task-owned temporary host was stopped and port 27917 was confirmed closed; its data, checkouts, installation, and audit records remain under `/tmp/cognia-github-bot-host`.

Live evidence: `/tmp/github-bot-headless-installation-final.log` (repeated after the final CLI rebuild and restart), `/tmp/github-bot-headless-installation-live.log`, `/tmp/github-bot-headless-installation-receipt.json`, and `/tmp/github-bot-headless-workspace-live.log`. The workspace log retains the initial failures followed by post-fix 200 results. The native server and CLI builds passed, and the generated companion contract check passed with 697 commands and 101 routes. Explicit JSON-null success responses now survive both the Headless bridge decoder and the desktop command boundary; malformed missing responses still fail.

No public test issue, comment, review, branch, or PR was created.

## Open acceptance boundaries

- **Headless execution:** owned workspace provisioning and service-only physical Git routing are implemented. Native host/Brain bootstrap and live filesystem/Git primitives were exercised. The rebuilt host passed fresh guarded clone, exact-SHA fetch/checkout, recovery of an existing checkout, and snapshot Git reads against the target repository. The live clone-output and refspec-input contract regressions are fixed and retested. Full paired UI execution remains a separate acceptance boundary.
- **Uncertain external turns:** sessions are persisted and reconnected, but the provider interface cannot authoritatively query a durable completed turn by dispatch identity. Such a turn is retained as an actionable blocked result; it is not automatically resubmitted. Automatic recovery to a completed result is not established.
- **Test evidence:** commands and exit codes in the plugin report are explicitly agent-reported. Tool events and the patch are available for inspection; this is not independent host execution of every reported command.
- **Production activation:** an updated Cognia Desktop or Headless host, an enabled GitHub Delivery account bound to the installation, and Devin readiness are needed before monitoring can be armed. Missing setup must remain visible.
- **Publication acceptance:** after an actual result is approved, verify the remote branch commit, exact PR/review contents, target branch, and resulting CI independently. That acceptance has not occurred.

## Repository-wide gates

The shared checkout contains hundreds of concurrent changes. Scoped lint and tests passed, but the global gates are not green:

- The actual 16 GiB `pnpm typecheck` reported existing errors across unrelated skills, teams, gateway, and plugin code. Task-specific errors found during the run were fixed. RTK's filtered typecheck output was insufficient; the raw script output is the source of truth.
- `plugin:author-imports` reports six existing violations in the VS Code template activation test and `kimi-subscription` tests.
- Full lint exhausted its heap while scanning the large shared tree/cache artifacts.
- Full repository coverage encountered unrelated failing suites and was stopped after disk exhaustion. A global coverage result was not obtained. The final four-file core runtime coverage exceeds 90% per file; this and other scoped coverage must not be presented as a global pass.
- The full Next production build was not run after the disk repeatedly reached ENOSPC. The static-export source audit is a separate, narrower passing check.
- The newly added Rust companion run-detail RPC test was not executed within the available disk budget.
- The initial Git fetch-refspec Rust fixture compile hit ENOSPC. The later native server build compiled the updated Git code. A local bare-Git fixture verified exact-SHA retrieval for a commit available only through a PR ref; live Headless exact-SHA fetch and checkout subsequently returned 200 and preserved the requested HEAD.

Only task-generated development output and the incremental caches of crates compiled for this task were removed to recover space. The smoke-tested launcher executable and concurrent source edits were retained. No commit or push was performed.

## Rollback

Disable the Bot installation and its triggers through the existing Bot controls. This stops new dispatch and cancels active execution while preserving run records, approval snapshots, and publication history. Do not delete those records to roll back an installation. Already-published GitHub artifacts require separate deliberate handling.

## Local user trial — 2026-09-12

This later acceptance supersedes the earlier fixture's unbound-account state. The user explicitly authorized reusing the local `MaxQian888` GitHub account. Its credential was transferred directly into the Headless encrypted store and connected through the existing authentication and integration-account lifecycle, without recording the token in scripts or logs.

- Browser: `http://localhost:3000/bots?bot=boti_9b814e3c-6db7-4b35-8261-7937209b0b67`, paired to the local Headless host. HTTPS host port: 27890; loopback browser listener: 27891.
- Persistent trial data: `.cache/github-devin-bot-trial`. This is a development service, not an installed system daemon. Web and Headless processes were left running for the user.
- Installation is enabled with the GitHub credential bound and healthy, repository `NJUPT-SAST/sast-approval-next`, model `swe-2-medium`, 30-minute execution ceiling and two repair attempts. Devin login and exact SWE-2 model availability were checked.
- Automatic 60-second polling repeatedly completed against the real repository. The paired browser displayed **Up to date**, completed runs, and zero dead letters. Historical items were not backfilled. No webhook endpoint was configured; the event trigger is armed for a future existing ingress connection.
- Real restarts verified account/binding/cursor retention, automatic restoration of GitHub Delivery before its dependent Bot, and browser reconnection. Both the manual scan RPC and the browser's **Run now** control were exercised after the runtime ownership fix.
- Startup acceptance found and fixed missing Headless runner ownership registration, remote plugin toggles that did not persist canonical intent, and restart filtering that ignored an explicit opt-in for manual-only plugins. The relative-time display now supplies the shared refreshable locale clock.
- Added internal-only reference-based account setup: 22 boundary tests, 100% lines/functions and 98.03% branches. Startup/bridge/manager suites: 375 passing tests; Headless Bot startup coverage: 100%. Bot display/detail: 20 passing tests, 99.59% lines and 96.15% branches. Scoped lint, formatting, companion contracts and the native/CLI builds passed. These remain scoped checks, not a new repository-wide gate result.

Trial logs are `/tmp/github-bot-trial-web.log` and `/tmp/github-bot-trial-host.log`; the non-secret final health receipt is `.cache/github-devin-bot-trial/trial-health.json`. Keep private credential/debug environment files out of reports and version control. Generated-code publication and approval acceptance against real GitHub remain pending an actual user-approved result; this trial created no public issue, comment, review, branch or PR.

## Issue 23 execution diagnosis — 2026-09-12

At the user's explicit request, a public test issue was created: https://github.com/NJUPT-SAST/sast-approval-next/issues/23. It asks only for deterministic boundary tests in the existing datetime suite. The monitor detected it, acquired an isolated checkout, and invoked `swe-2-medium` in session `cactus-candle`.

The real execution exposed two defects missed by the earlier synchronization acceptance. The Bot selected Devin `ask` mode, which exposes read tools but omits editing and execution tools. Devin read two files, drafted an unapplied patch, and honestly reported that it could not run tests. Its response contained prose followed by one valid fenced JSON report. The plugin attempted to parse the entire response as JSON and dead-lettered the delivery after five attempts. The attempts reused one checkpointed model response; there were not five model executions. The owned checkout remained clean and nothing was published.

The parser now accepts a single wrapped report, rejects ambiguous reports, and preserves invalid completed responses as inspectable blocked results with session and artifact evidence. Missing or failing test evidence still prevents publication. Six plugin suites pass (75 tests; 99.91% lines, 95.92% branches, 100% functions). The real response shape is covered by a regression that preserves the completed first attempt on replay.

An independent notification contract defect was also found: the TS publisher and native handler support `source`, but the closed request schema omitted it. The schema now matches the native bounded field; 49 contract tests and the native build pass. Routine Bot delivery ticks now suppress start/completion notifications, including migration of existing task rows; 19 focused tests pass with 98.93% lines, 93.65% branches and 100% functions. A later full contract freshness check encountered a concurrent undeclared `/integrations/lark/workbench/resolve` route; that shared-tree failure is separate from these passing checks.

The Bot now selects `acceptEdits`, while disabling ACP host-terminal delegation for isolated Bot processes. A real SWE-2 smoke run wrote a fixture, executed its check after a single command permission, and confirmed that outside-workspace reads and writes were denied. The first real retry, session `imported-olive`, wrote 41 lines to the datetime test file but exposed an unconnected native command permission. Disabling the installation cancelled that run and retained its checkout.

Live command permissions now enter the existing persistent Bot interrupt controls, with one-shot grants, request deduplication, ownership revalidation, cancellation, and expiration. The ACP/API suites pass 208 tests; external-agent API coverage is 99.72% lines, 95.36% branches, and 100% functions. These live decisions expire after four minutes and still count against the execution timeout; the independent publication decision retains its seven-day lifetime. Excluding live command approval waits from the execution clock remains incomplete.

The reused run-detail UI distinguishes a command request from publication contents in English and Chinese. Its 27 tests pass, with 95.62% lines, 91.39% branches, and 92.85% functions in focused coverage. The focused coverage invocation also reports missing unrelated global coverage groups, so it is not a global coverage gate pass. Locale freshness/parity and scoped UI lint pass.

Terminal delivery replay also reported success while the runner refused an already-terminal execution. It now throws a typed, actionable refusal before changing the delivery; nonterminal recovery still reuses its checkpointed record. Its 22 tests pass with 98.94% lines, 92.1% branches, and 100% functions. A complete Bot Retry action that creates a new linked execution remains separate work; this trial used explicit new manual executions.

A second live retry, session `silken-plate`, recorded the exact test command in a persistent pending interrupt. Browser inspection found that the run snapshot had replaced its long interrupt identity with an opaque identifier, preventing the UI from matching it to its decision record. Bot approval IDs now use SHA-256 over the structured run/step pair; old decisions retain an exact legacy lookup with run ownership validation. Generic journal text redaction remains intact. The paired control handler also now uses the native-verified `callerDeviceId`, rather than the different, caller-controlled `deviceId` field. Seventy focused tests pass, including real database/journal/control integration and spoofed-device rejection; the step and handler modules exceed 90% in all measured coverage categories.

During host restarts, the browser displayed a protocol-incompatibility message. A later restart reproduced it with matched binaries, and reloading after the host became ready restored Online without rebuilding again; a genuine version mismatch was therefore not established. The startup/reconnect presentation still needs separate investigation. Native builds were refreshed for the actual native schema and sandbox changes. The paired account binding remained intact. No generated code, review, comment, branch, or PR has been published. The earlier successful repository synchronization must not be interpreted as completed implementation or publication acceptance.

The paired Approve button then exposed HTTP 428: run controls omitted the required device-bound approval lease, and their rejected promise was not rendered. Explicit remote clicks now acquire the existing exact-operation 120-second lease; unavailable/local controls do not acquire one. Consent challenges and failures render an outcome, including an optional host authorization code. Hook verification passes 25 tests with 100% lines/functions and 91.66% branches; UI/catalogue verification passes 36 tests with 95.65% lines, 91.39% branches and 92.85% functions. Scoped lint and locale freshness/parity pass.

Live session `jelly-form`, run suffix `7c875f7e-47ef-44d8-87c7-aaeac699bc12`, verified the paired click through persisted approved interrupt to actual Devin command execution. Dependency installation, the scoped datetime test, and a Jest retry using a cache inside the isolated checkout were individually reviewed and approved. This did not change Devin to bypass mode and did not approve any publication. The first pending command expired during diagnosis and was denied; a later distinct command continued normally.

The model reported 20 passing datetime tests after moving Jest's cache inside the checkout. An independent local rerun, `pnpm test --runInBand --cacheDirectory ./node_modules/.cache/jest lib/__tests__/datetime.test.ts`, also passed all 20 tests with exit 0. The run nevertheless failed result capture because generated `.pnpm-store/` files were untracked and exceeded the complete-file capture limit. Only that run's generated package cache was removed; the test change and execution records were retained. This failure is separate from the now-verified approval click.

Temporary and npm/pnpm cache paths now derive from the existing owned runtime state directory. Both modern pnpm configuration names and legacy npm-compatible names are set, because a live pnpm configuration probe showed version-dependent behavior. No writable sandbox roots were added. Workspace provisioning preserves `.git/info/exclude` while adding generated-cache patterns. A real Git fixture confirms that untracked generated caches are omitted, while modified tracked and force-staged cache files remain in complete capture. The active host's real workspace filesystem commands also passed exact excludes write/read with previous content preserved. Workspace verification passes 25 tests at 100% lines/functions and 93.33% branches; the two focused Rust cache/environment tests and launcher build pass. Snapshot completeness checks remain enforced.

The subsequent live run exposed the separate Headless Node launch path: it still set only XDG variables, so the native helper alone did not change Devin's actual temporary directory. The existing Node environment builder and directory provisioning now share `botRuntimeEnvironment`. Sixty-nine focused tests pass, including a real Node backend -> configured native launcher -> confined child process that verifies seven owned, writable temp/cache paths and credential scrubbing. The Node backend's full existing file remains below the 90% coverage gate (88.02% lines, 75.30% branches, 100% functions; uncovered DSH/lifecycle paths); this gate is not claimed as passing. The sandbox-launcher helper reaches 99.59% lines, 97.01% branches and 100% functions. CLI and native/launcher artifacts were rebuilt, and the trial host now loads the final Node wiring.

## Pending real publication — 2026-09-13

Session `verdant-buffer`, run suffix `7c9ce305-3018-4948-b17d-1d9e011ba6b7`, reached publication approval. The run used an explicitly owned temporary directory while the final Node wiring was being corrected. Its report records 12 passing datetime tests and clean test-file ESLint; an independent local rerun of the canonical `pnpm test --runInBand lib/__tests__/datetime.test.ts` also passed 12/12 with exit 0. This is a new test implementation; the previous trial's 20-test result belongs to its retained earlier checkout.

The immutable snapshot `e13344da4ddfbb95f42a49104c73ac0349d28aad562512794c947b5e155005c7` contains only `lib/__tests__/datetime.test.ts`, against base `b5b9ff62b1f2ba8dca4faafa9c4a3df900e39085`. Approval `bot-approval:76503770abe98493753411ee7673ab15b0bcb7546691ebbf50a8ccee8044bd98` remains pending, expiring at `2026-09-19T16:45:56.049Z` (UTC). A real host restart preserved the exact pending approval and snapshot without another model dispatch. No publication approval was submitted by the assistant, and no branch, PR, or review/comment was published. The user must review the concrete diff and proposed PR before publication acceptance and subsequent CI verification can continue.

The Web and Headless trial services remain running with monitoring enabled. Historical failures and dead letters are retained. After restarting the host, reload the browser once the brain is ready if its startup-time protocol message persists. These live results do not supersede the unrelated full-repository gate failures listed above, the incomplete Bot Retry action, or the live-command approval-time accounting limitation.

The pending publication exposed one further recovery defect: each resume produced a new journal event, but repeated parking reused a fixed `run.waiting` source ID. Journal deduplication left the projected snapshot running while the stored row was waiting, hiding Approve/Deny. Parking now includes the attempt's starting journal revision in its occurrence identity. The regression failed before the fix and passes across three re-entries with one unchanged approval, snapshot, expiry, and no charged delivery attempt. The run/step/control suites pass 73 tests, including recovered cancellation and expired-lease checks; `run.ts` reaches 100% lines/functions and 91.2% branches. Scoped ESLint and formatting pass.

The trial CLI JavaScript bundle was rebuilt and the owned host restarted. Normal recovery repaired the existing run without a database rewrite: revisions 114 and 120 had both row and snapshot waiting, with approve/deny/stop/open_details available across later re-entries. The paired browser was Online and displayed enabled Approve and Deny buttons for `verdant-buffer` under the publication review section. The exact pending approval and snapshot above were retained. No publication button was clicked. This local JavaScript build uses the previously built debug native helpers; a full packaging invocation separately reported a missing release launcher and is not claimed to pass.

## Unattended execution and automatic reviews — 2026-09-13

The user subsequently authorized unattended execution and automatic PR/review publication. This supersedes the earlier human-publication default for this installation only; automatic merging remains disabled. Existing installations retain approval defaults until explicitly configured.

The plugin now separates `executionMode` (`approval` / `unattended`) from `publicationMode` (`approval` / `automatic`). The existing Bot settings form also records explicit host permissions in `installation.policyGrant`, separate from plugin-controlled config. Plugin defaults cannot grant authority. Host ceilings, the original run policy checkpoint, owned workspace checks, and current grant revalidation govern execution and publication. Revoking unattended authority cancels active execution through the existing runtime signal. Bots without plugin configuration schemas can still revoke host permissions through the reused settings form.

Unattended Devin uses the existing `bypassPermissions` adapter mode inside the strict Bot sandbox. Model and mode remain pinned to the owned session; there is no silent model fallback or permission upgrade of a recorded turn. A real SWE-2 Max smoke run edited a fixture and executed `node verify.cjs` with zero permission requests; synthetic credential reads and outside-workspace writes were denied. Evidence: `/tmp/github-bot-devin-unattended-smoke.log`.

Automatic publication creates an immutable decision in the existing interrupt/journal system with explicit host-policy provenance. It remains bound to the exact snapshot, publication intent, actions and target SHA. Current policy is rechecked at publication; denial, cancellation, expiry, read-only ceilings and revoked grants remain blocking. Older human decisions are not automatically converted.

PR reviews now carry structured COMMENT / REQUEST_CHANGES / APPROVE verdicts and validated inline findings through `github-delivery`. Subsequent commits use the prior reviewed SHA for an incremental diff. Clean reviews of the authenticated account's own PR, or an account whose actor cannot be verified (including unsupported GitHub App identity), produce an explanatory COMMENT rather than APPROVE. CI reconciliation ignores failures superseded by successful reruns and correlates self-produced CI only with recorded publication revisions; no merge action is introduced.

Focused verification: 86 settings/lifecycle/console tests; 154 host policy/ownership/publication tests; 33 external-agent API tests; 166 plugin/provider tests; 88 builtin metadata/distribution tests; 50 companion contract tests; 14 Python Bot and 24 Python external-agent serialization tests. Changed governed modules in those coverage runs individually exceed 90% lines, branches and functions. Locale freshness/parity, scoped ESLint/formatting, SDK author-types and plugin contract checks passed. Both plugin archives match their manifests and bundled entries.

Full typecheck still reports unrelated shared-tree diagnostics; its one newly reported plugin-test tuple typing issue was corrected. The full companion generator/check remains blocked by the independently added undeclared `POST /integrations/lark/workbench/resolve` route. The Bot request-schema/identity artifacts were refreshed from the generator's inspected output while retaining that validation failure, rather than changing the unrelated route. Author-import failures remain in existing Kimi/VSCode fixture imports. These scoped results do not constitute a passing full-repository coverage, lint or static-export build gate.

The real paired browser saved `swe-2-max`, unattended execution, automatic publication, and explicit `bypassPermissions` / `autopilot` / no-human-write-approval grants. The host retained these settings across restart. Test issue #25 was automatically detected under the previous mode; that earlier `copy-edge` run is retained after its command approval expired. A later fresh run exposed a stale native launcher rejecting `--bot-isolation` before Devin session creation. Rebuilding the existing launcher restored the isolated startup path; no sandbox restriction was removed. Live unattended publication acceptance is recorded below when the fresh execution completes.

The stale-helper failure now has a Bot-only parser capability preflight in the existing launcher resolver. An unsupported selected launcher produces `BOT_ISOLATION_LAUNCHER_UNSUPPORTED` and `needs_setup` before agent dispatch, with no fallback. The fresh debug helper accepts the flag; the stale bundled helper was verified to reject it. The trial keeps its explicit debug-helper override. Launcher/API coverage passed 69 tests (both files above 95% lines and branches); the Headless launch test confirms that the override is inherited.

The first complete unattended run (`8517fc82-3aef-44b5-a882-0dfaf24c4dee`, session `same-dance`) executed SWE-2 Max without any permission interrupts. It changed only `lib/__tests__/navigation.test.ts` (61 insertions), reported successful scoped Jest, ESLint, formatting and typecheck commands, and retained an immutable patch. A separate rerun of its scoped Jest suite passed all 40 tests. Publication correctly remained blocked because the implementation JSON omitted a review-only string required by the original parser. This is a report-contract defect, not a failed code execution; the original terminal run and evidence remain retained.

The report parser now validates by task: implementation/repair may omit the unused review field; PR reviews must provide a nonempty review. Wrong field types, ambiguous JSON and missing/failing test evidence still fail. The final Bot suite passes 121 tests, with every plugin file above 90% in lines, branches and functions. Both APPROVE and REQUEST_CHANGES on a self-authored or unverifiable-author PR become an explicit COMMENT retaining the findings; verified third-party verdicts remain intact. GitHub documents the self-approval restriction at https://docs.github.com/en/pull-requests/how-tos/review-pull-requests/reviewing-proposed-changes-in-a-pull-request.

The earlier user-approved issue #23 result has now been independently verified as PR #24, commit `1bd5e08ab14b2bba24234f9b0931063c59029be6`. All 13 returned GitHub checks completed successfully, including the test suite, quality/security, CodeQL and platform builds. This establishes that earlier human-approved publication and its CI; it does not establish automatic publication for issue #25.

Manual command approval remains a separate known limitation: the shared decision waits four minutes, the native ACP permission request expires after five minutes, and Bot/manager/JSON-RPC execution clocks remain wall-clock deadlines. A correct seven-day resumable command approval requires coordinating those lifetimes; raising one TTL would not satisfy the contract. The configured unattended mode does not create those command approval waits. Human publication decisions already have seven-day persisted parking.

The terminal Retry action now uses the existing delivery RPC to create one deterministic linked successor, preserving the old journal, workspace and decisions without copying approvals. Resumable dead letters retain their existing identity. Its first coverage pass completed 72 tests across five suites; local control writes and delivery UI exceed 95% branch coverage. The real paired click then exposed the existing outbound key format defect (`a UUID Idempotency-Key is required for this command`) and an incorrect failure toast for a queued remote receipt. Those transport/UI regressions are being verified separately before live retry acceptance. The Retry description was updated in the existing serialized catalog and generated API artifacts; native server build passed in 50 seconds and companion contract tests passed 50/50. Full contract validation still reports the same undeclared Lark route.

The complete Retry slice passes 105 tests across six suites. Every changed governed file individually exceeds 90% lines, branches and functions (aggregate 99.26% lines, 95.47% branches, 100% functions). New explicit Bot control presses use fresh transport UUIDs; retrying the same persisted outbound job retains its UUID. Host delivery identity independently deduplicates the linked successor. Exact legacy Bot queue keys normalize per queue row without changing unrelated commands. Fresh presses recover from an earlier cached startup-time 503, while stable successor identity prevents duplicate runs. The queued UI receipt and typed unavailable errors are localized in both languages. Scoped lint, formatting and locale checks pass.

The trial host accepted the existing delivery Retry command and created one linked successor for issue #25, session `early-thorn`, using SWE-2 Max and `bypassPermissions`. The paired browser displays links to both original and successor runs. A fresh repository-scan request sent through the actual paired UI reached `sent` with zero transport retries. The earlier paired Retry requests remain audited after UUID and startup-time bridge failures; they are not presented as successful UI dispatches.

## Real automatic publication acceptance — 2026-09-13

Issue #25 completed in the linked `early-thorn` run with `swe-2-max`, zero command approval interrupts, and an approved `policy` decision (`bot-approval:5b9e99852e5d1b250bcebe0108d8d4ab0d450f685a1e8c06d713a08a2926f971`). The host published snapshot `fcda5d7eae22dd66332bcc25fd57e380629061ab2a31e36f97dd70ef3f59ec9b` as PR #26: https://github.com/NJUPT-SAST/sast-approval-next/pull/26. No human publication button was clicked.

Remote verification confirms open PR #26 targets `master`, branch `codex/github-devin/issue-25-b5b9ff62b1f2`, head `a019bb4675910102adf2240a1666bf0824fb97e8`. Only `lib/__tests__/navigation.test.ts` changed (73 insertions, no deletions). GitHub and the retained workspace both report blob `1cbfc360c46e1ef716ddc296fd0ebef5ec8f2285`, proving the published file matches the captured result. Independent rerun of that workspace passes all 39 tests. The agent also reported successful ESLint, Prettier and typecheck. CI started on the published head; its final state is recorded separately below.

Transient Bot agents now release their existing manager connection after durable result capture, including failure/cancellation and failed-cleanup recovery paths, while retaining owned workspaces and provider-session checkpoints. Forty-four API tests pass, including twelve consecutive invocations against a ten-agent limit and same-session re-add/resume. API coverage is 99.78% lines, 95.09% branches and 100% functions. The final CLI bundle was rebuilt and the host restarted after issue #25 completed; the installation returned enabled with a successful synchronization.

GitHub CI acceptance for PR #26 completed: all 12 returned checks are successful, including Quality, Test Suite, Test Results, CodeQL and its Actions/JavaScript/Python/Rust analyses, GitGuardian, and Windows/macOS/Linux Tauri builds. No automatic merge was performed. Scoped ESLint was also rerun independently on the retained published file and passed.

Completed-result inspection now feeds retained Bot snapshots and validated command reports into the existing Changes and Tests tabs. Thirty-three run-detail tests pass (96.18% lines, 93.84% branches, 94.44% functions), with locale/lint checks passing. Actual paired-browser verification shows Changes 1 with the source-control diff viewer, Tests 5 with commands/exit codes/output and explicit agent-reported provenance, and an approved publication record with no pending action buttons. Screenshot: `/tmp/github-bot-pr26-changes.png`.

A subsequent post-publication restart probe found lost plugin metadata: Headless used an in-memory localStorage shim for ctx.storage, so the published PR CI remained recorded as pending after remote CI completed. The authoritative host publication checkpoint and result survived. Durable plugin storage and recovery from those owned publication references are required before claiming restart-safe CI tracking; the passing remote CI alone does not establish that behavior.

## Durable monitoring recovery — 2026-09-13

Headless plugin storage now uses the existing owned PluginRow with transactional writes, a bounded quota, caller-only legacy namespace migration, and error propagation. Rediscovery preserves storage; paired registry synchronization strips this private field. Browser/desktop storage remains compatible. The additive Bot publication references expose only locally owned, repository-matched publication/snapshot metadata, with SDK author declarations, contract catalog and Python serialization updated together. Focused storage/DB/API/sync verification passes 139 tests and Python passes 39 tests. Storage, Bot API and plugin CRUD each exceed 90% lines, branches and functions. The existing large desktop-sync-source module remains below the overall coverage threshold despite its passing private-storage regression, so that broader gate is not claimed to pass.

The monitor restores self-produced PR correlation only when the remote repository, branch, exact head SHA and work marker match a host-owned publication reference. Unresolved or cyclic repair lineage conservatively exhausts its budget. CI status and failure detection now share latest-per-workflow/check selection: obsolete pending attempts cannot override a newer success, while genuinely queued checks remain pending. All 142 Bot tests pass; monitor and GitHub helper coverage exceed 95% lines/branches and reach 100% functions. The plugin package was rebuilt after these changes.

Publication recovery traverses checkpoint primary keys in pages of 512 and loads only publication bodies before their ownership joins. A 1,200-empty-poll regression verifies one run read and one delivery lookup for the valid old resource-less publication. The API suite passes 7 tests with 99.51% lines, 93.15% branches and 100% functions. Index-key traversal remains linear in retained checkpoints; this is bounded-memory recovery, not constant total work. A read-only follow-up review confirmed both the CI classification and per-poll query findings were addressed.

The first live restart recovered PR #26 from host checkpoints, persisted five plugin storage keys, and emitted a single transition to `ciStatus: completed` for head `a019bb4675910102adf2240a1666bf0824fb97e8`. Its metadata retains parent issue 25 and zero repair attempts. The total external-agent dispatch checkpoint count remained 12; no duplicate model execution or publication was needed. The paired browser returned Online and retained Changes 1, Tests 5 and Approvals 1. A second restart with the final API performance fix is verified separately below.

The second host reached runtime readiness at `2026-09-12T18:25:50.350Z`; a subsequent successful synchronization was persisted at `2026-09-12T18:25:53.485Z`. At `2026-09-12T18:27:00.735Z`, all five plugin storage keys and the exact completed PR #26 tracking record remained present. The CI event history still had one initial pending event and one completed transition, and external-agent dispatches remained 12. A fresh GitHub query found exactly one PR for the published branch, still open as #26. Final JavaScript build and scoped diff checks passed. The paired Bots page is Online, with the installation Enabled and all five triggers armed. Its two historical dead letters are retained and still contribute to the Needs attention badge; they do not indicate a new synchronization failure. Nonsecret restart receipt: `.cache/github-devin-bot-trial/unattended-restart-acceptance.json`.
