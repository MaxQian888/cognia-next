{
  "name": "@cognia/tts",
  "version": "0.0.0",
  "private": true,
  "description": "Cognia framework-agnostic text-to-speech core: provider adapters behind a registry, streaming PCM player, chunk pipeline, retry, cache, text utils, and the speech-settings adapter — platform bridges injected via the TtsHost seam.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "cognia-source": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "cognia-source": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@tauri-apps/api": "*"
  },
  "peerDependenciesMeta": {
    "@tauri-apps/api": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
