{
  "name": "@cognia/companion-client",
  "version": "0.0.0",
  "private": true,
  "description": "Cognia companion device client: the P-256 DPoP proof, challenge/token exchange, typed refusals, and the Browser Companion wire contract — shared by the app renderer, the CLI worker, and the browser extension.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "cognia-source": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "cognia-source": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
