{
  "name": "@cognia/diagnostics",
  "version": "0.0.0",
  "private": true,
  "description": "Cognia diagnostic vocabulary: one serializable diagnostic record plus the code registry that supplies its severity, retryability, icon token and default user actions. Framework-agnostic and i18n-free — renderers map codes to localized text.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "cognia-source": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./*": {
      "cognia-source": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
