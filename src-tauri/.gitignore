# Cargo target/ now lives at the workspace root (../target/) — see the root
# Cargo.toml and root .gitignore. This file only covers files Cargo or
# tauri-build still drops inside src-tauri/ itself.
/gen/schemas
