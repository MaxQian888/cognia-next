---
"cognia-next": minor
---

Built-in agents: a read-only `code-reviewer` joins Explore and Plan on the dispatch and CLI surfaces, returning severity-ordered findings with `file:line` and the smallest fix. The general-purpose, Explore and Plan prompts now carry working method only (the runtime frames the dispatched-subagent contract), and every non-workflow built-in has a colour so the CLI's live rows and the app's pickers tell them apart.
