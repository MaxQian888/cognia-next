---
"cognia-next": minor
---

Redesign the AI Connections and Model Catalog settings: the provider Models tab is a compact sortable table with capability glyphs, context / output / price columns and a compare column; the model comparison is fed from those checkboxes, groups its rows, marks the best value per row and can hide agreeing rows; the provider rail uses quiet status dots and a tinted selection; narrow panes push list → detail instead of opening a drawer.
