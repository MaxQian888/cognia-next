---
"cognia-next": patch
---

Shared-chat realtime now opens through the shell's proxy-aware transport, so a desktop Host behind a proxy keeps live session updates instead of losing them while every other request works.
