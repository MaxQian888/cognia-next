---
"cognia-next": patch
---

Chat composer: keep the send icon while a turn is running whenever there is something to send. Typing a follow-up mid-turn now shows Send (it joins the running turn) instead of forcing Stop, and the button falls back to Stop the moment the box is empty again.
