---
"cognia-next": patch
---

Redesign the collapsed sidebar (guild rail): unified 36px icon-button geometry under the panel radius with all glyphs at 18px, hairline group dividers shortened to nub length, the workspace switcher drawn as a tinted chip, and the active destination now carrying both the traveling selection tint and a detached edge bar on the window edge — so "where am I" reads at a glance instead of relying on a faint fill.
