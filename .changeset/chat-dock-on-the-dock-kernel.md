---
"cognia-next": minor
---

Chat's right-hand dock now runs on the unified Dock kernel: panels can be reordered, dragged between groups, split and merged instead of one showing at a time. Your open panel and the width you dragged to are carried over from the old workbench on first open, and the previous layout stays available via `cognia-dock-kernel-surfaces-v1`.
