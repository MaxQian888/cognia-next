---
"cognia-next": patch
---

Sites console: the versions list no longer loads each build's whole archive out of the database to show its size and file count — the two numbers are recorded on the version itself and backfilled for existing builds.
