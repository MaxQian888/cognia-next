---
"cognia-next": patch
---

Sites console: a long build no longer disables unrelated controls — busy state is tracked per action, double-clicking an action no longer runs it twice, and Upload is disabled with its own reason when wrangler is missing instead of failing on click.
