---
"cognia-next": minor
---

Plugins can declare installable Bots. A Bot binds an event source, a policy ceiling, an executor (a published workflow, a Squad, one agent turn, or a durable handler the plugin ships) and its credential slots into one named, versioned unit, authored in TypeScript or Python and validated at both load time and lint time.
