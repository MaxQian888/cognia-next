---
"cognia-next": patch
---

New instructions sent mid-turn now supersede pending approval requests: each outstanding approval is denied through its own channel and the card stays as a terminal "superseded" notice instead of remaining answerable, while the command judge's cached verdicts are re-scoped to the new instruction context.
