---
"cognia-next": minor
---

Performance panel: add a System tab (host OS, kernel, CPU model and core count, installed memory, app/Tauri version, build profile and enabled features), keep the chosen sampling cadence across visits, stop the rolling graphs from silently mixing sampling rates after a cadence change or a pause, make every sortable column header keyboard-operable, and remove the per-frame O(n²) recomputation behind the process table's trend sparklines.
