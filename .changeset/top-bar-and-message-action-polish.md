---
"cognia-next": patch
---

Chrome polish: the message action row now ends with its overflow "…" menu instead of parking it between the single-purpose buttons; on macOS the native traffic lights are vertically centred in the 40px top bar (they sat against the window's top edge) and the bar reserves 88px for them so the app icon no longer touches the green button; and the conversation header's shared/private control is icon-only, so the top bar's widest label — a whole disabled sentence when collaboration is switched off — moves to a tooltip.
