---
"cognia-next": minor
---

Rebuild the Account, Agent modes, Built-in agent runtime, Eval and Memory settings pages without card wrappers — Agent modes, the agent runtime and Memory become provider-style master/detail surfaces that fill the settings frame and scroll inside the detail pane instead of scrolling the page, the rest are flat stacks with collapsible advanced groups, and every layout now sizes off its own container so it adapts inside a narrow detail pane
