---
"cognia-next": minor
---

Built-in browser: a fresh `/browser` pane opens on its empty state instead of the "open in another panel" screen; the tools dock stays one row in a narrow chat rail, opens from any tab, shows a running recording while collapsed, and keeps a take in progress and a developer-mode grant alive when collapsed or switched away from; developer mode opens directly instead of behind a second disclosure; the cloud browser maps clicks through its letterbox, labels its connection state, can switch back to the built-in engine, and polls devtools slowly while the dock is closed; the web preview shows load progress and recent pages. Recent pages now persist per account across restarts and appear on the empty state. Browser data: imported Chromium sign-ins can be removed ("Sign out in the preview"), "Clear all data" also signs the preview out of every site, recorded flows are included in backups, and deleting a cloud-browser profile erases its data on the server.
