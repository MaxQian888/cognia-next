---
"cognia-next": minor
---

The scheduler panel now shows who set a task up, links straight to the conversation, goal, plan or squad a run produced, tells you when a task could not be created instead of silently closing the sheet, offers a real character picker for goal tasks, and gives background commands a proper editor instead of raw JSON.
