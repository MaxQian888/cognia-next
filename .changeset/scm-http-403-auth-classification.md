---
"cognia-next": patch
---

Source Control: an HTTP 403/401 from a git remote (an insufficient or expired token — common with GitHub) is now surfaced as an authentication error with a credential CTA, instead of being misclassified as a network failure that pointed users at connectivity troubleshooting.
