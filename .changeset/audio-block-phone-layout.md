---
"cognia-next": patch
---

Chat: the audio attachment player now fits on a phone. Its control row sized itself to its contents and could not shrink, so on a 375px screen the duration readout and the mute button were clipped outside the card and unreachable. The row now reclaims width (tighter control padding, a smaller cover, less card chrome) and moves the seek bar onto its own full-width line — no control is hidden, and the desktop layout is unchanged.
