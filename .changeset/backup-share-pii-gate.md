---
"cognia-next": patch
---

Backup share links are scanned for personal information first: a plaintext backup shows a per-area hit report that must be confirmed before the link is created, an encrypted backup is labelled as unscannable, and the shared package now honours the selected encryption mode.
