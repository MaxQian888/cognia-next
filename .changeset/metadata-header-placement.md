---
"cognia-next": patch
---

Chat: setting a message's Usage or Cost metadata to the "header" placement now actually shows it in the message header. Both fields offered the same three placements as every other field, but the header only ever rendered six of the eight, so choosing "header" for usage or cost silently displayed nothing.
