---
"cognia-next": minor
---

A paired phone or browser can move files over the SSH hosts this machine has saved. File transfer is its own `ssh.files` grant on the device console rather than something the terminal grant quietly includes, and the row says out loud that it reaches those machines without a folder limit, because SFTP paths are the remote machine's own absolute paths.
