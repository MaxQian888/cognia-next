---
"cognia-next": minor
---

Scheduled tasks now live in your account's database instead of a separate machine-wide one. They are encrypted at rest, isolated per account, and included in backup and export. Existing schedules are adopted automatically on first launch.
