---
"cognia-next": minor
---

Provider and model metadata now resolves offline: the models.dev catalog and provider brand icons ship with the app instead of being fetched at runtime, so provider pickers, cost estimates and capability checks work with no network. Routing gains a shared attempt controller, difficulty-based presets and deployment filters, and routing test runs are saved so a calibration can be compared against a later one.
