---
"cognia-next": minor
---

IM connectors: the inbound trigger policy is now editable, per bot and per conversation. Nine of the eleven platforms also shipped a default that could never answer in one of its chat scopes — an @-mention of a Telegram/Slack/Discord/Lark bot in a group, and a plain direct message to a WeCom/OneBot bot, were both dropped unanswered. The conversation header's policy read-out now shows the policy the conversation actually evaluates instead of a hard-coded default.
