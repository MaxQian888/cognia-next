---
"cognia-next": minor
---

Feishu principals now name a person, not the local profile. The identity registry mints or finds a real `User` for every bound sender, converges a Lark identity with the web sign-in it belongs to, and the principals card shows who each principal is and what access they actually have.
