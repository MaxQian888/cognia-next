---
"cognia-next": minor
---

Artifacts: point at an element in the preview to reference it in chat. The picker works on every rendered surface — charts and diagrams, static HTML/SVG, and sandboxed React artifacts — and a picked element is staged as an edit target, so the reply can come back as a revision proposal. Hold ⌘/Ctrl while clicking to send it straight to the conversation.
