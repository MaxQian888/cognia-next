---
"cognia-next": minor
---

Open desktop handoff sessions directly in a dock terminal running `cognia-agent resume`, with an availability check and an interactive prompt when no continuation prompt is supplied inline.
