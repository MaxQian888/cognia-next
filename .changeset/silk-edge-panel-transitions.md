---
"cognia-next": patch
---

Smooth the sidebar and artifact-dock collapse/expand animations: both gestures now run as one shared View Transition over the rail, panel, workspace and title-bar outlets, so the header zones morph in lock-step with the columns instead of flashing or snapping mid-motion.
