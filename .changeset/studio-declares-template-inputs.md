---
"cognia-next": minor
---

The Template Studio can declare a template's inputs. The draft editor now owns the id, label, type, required flag and per-type extras, and it reads the payload back — tokens nothing declares are listed with a one-click "declare them", using the same classifier that refuses them on save, so a workflow expression is never mistaken for a missing declaration.
