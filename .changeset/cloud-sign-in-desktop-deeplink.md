---
"cognia-next": patch
---

Desktop cloud sign-in finishes on its own. The `cognia://logto/callback` deep link the system browser lands on now reaches the sign-in screen, so the code is exchanged without pasting the address back. Pasting stays available as the fallback when the browser never returns.
