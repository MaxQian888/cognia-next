---
"cognia-next": minor
---

The three routes you actually want to check from a phone now work on one. Source control gets a phone body: the change list is the page, the diff opens as a drawer, and pull and push carry their ahead and behind counts, instead of a resizable two-pane split squeezed into 375px. `/logs` gets a definite height so its workspace stops collapsing, and drops the volume histogram on a narrow screen where it was a few pixels per bucket. `/integrations` gets phone-sized padding, having spent 13% of a 375px viewport on margins.
