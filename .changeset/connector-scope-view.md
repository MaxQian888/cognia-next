---
"cognia-next": patch
---

The connector detail (Settings → Connections → Config) now shows a read-only "Access" card listing the OAuth scopes a Slack/Lark connector was actually granted, with the grant time — so you can see at a glance what each connector is allowed to do.
