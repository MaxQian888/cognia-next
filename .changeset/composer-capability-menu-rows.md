---
"cognia-next": patch
---

Align the `+` menu's capability entries with the rest of the menu. Web search, Attach skill, and the room-target picker now render as full-width menu rows (muted icon, text-sm label, active dot) instead of small toolbar chips on a wrapping strip, on both the desktop popover and the mobile sheet; disabled rows keep their explanation (hover tooltip on desktop, a hint line on mobile).
