---
"cognia-next": patch
---

Directory Browse buttons now appear only where a folder can actually be chosen. Three of them rendered everywhere and did nothing when clicked: the external agent's working directory and extra skill roots, and the repository clone destination. The plugin devtools project picker threw an unhandled error on the web. The backup destination could not be set at all outside the desktop app, even though a headless host writes backups.
