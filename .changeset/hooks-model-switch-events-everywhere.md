---
"cognia-next": patch
---

The CLI now recognizes the `PreModelSwitch` and `PostModelSwitch` hook events, so hooks configured for them are validated and listed in `/hooks`. The CLI hooks panel shows a description for the built-in command auth gate instead of a raw message key, and the hooks settings report the pinned Claude Agent SDK 0.3.269 and its 33 events.
