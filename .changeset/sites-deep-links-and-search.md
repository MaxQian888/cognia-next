---
"cognia-next": minor
---

Sites are now linkable and findable. `/sites` accepts `?site=&tab=` and keeps the URL in step with what you are looking at, so a Site can be linked to and reloaded; and ⌘K finds Sites by name, worker name, production hostname, or attached custom domain.
