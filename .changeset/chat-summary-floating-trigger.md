---
"cognia-next": patch
---

Chat header: the duplicate participant chip is gone from the title bar — the same roster already lives in the summary card's Sharing row — and the summary opener no longer occupies the title bar's action zone. On the desktop workspace the opener now floats at the chat pane's top-right, beside the column the summary card docks into; embedded chat surfaces (workflow chat tab, resource workbench, mobile) keep the opener on their own header row. Split-view panes get one opener each, and the summary card's focus restore, Escape handling, and Manage action are unchanged.
