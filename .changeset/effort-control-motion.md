---
"cognia-next": patch
---

The thinking-level ("model strength") control now animates. The slider track carries a filled span that eases into the newly picked depth and the knob lands with a spring, so a tier change reads as a change in energy rather than a repaint; the chip's brain glyph pulses and its label rises when the level changes under it, which is often the only part of the control on screen. Ultracode — the tier that also arms the dynamic-workflow tools — is the one that keeps moving after it lands: its dot lattice drifts, a highlight sweeps across it, and the track breathes a violet halo. The tier chips in Settings → Conversation gained the same press-and-pop feedback. All of it is decoration, so reduced-motion settings collapse it without taking any state with it.
