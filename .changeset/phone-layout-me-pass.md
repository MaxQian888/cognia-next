---
"cognia-next": patch
---

Phone layout pass over the /me screens: the "desktop required" gate is now a centred empty state with a button that goes to pairing, storage / device info / sync / backup / notifications / computer use read as sections instead of stacked cards, the backup form stops centring itself and states the passphrase rule it actually enforces, sync rows show readable table names, the scheduler shows one All chip and intervals in days and hours, and a blocked /me sub-page offers the way back to /me.
