---
"cognia-next": minor
---

Desktop sidebar redesign (Codex-style nested groups): the merged rail carries no chrome at rest — search, filters and list actions moved into the title bar (magnifier opens the field row; `/` still works; Esc on an empty field folds it back). "New chat" is the only pinned row, flat like the navigation rows; everything else — nav, the non-collapsible CHATS label, one collapsible header per squad (avatar · name · hover "+" · unread pill · right-side chevron), "Show more" rows and Create team — scrolls together, with the footer anchored at the bottom. Chats previews four rows behind "Show all N", squads two behind "Show N more", and an empty Chats group offers a clickable "start one" row. Squad headers stay drag-to-reorder with pointer-only drag so Enter/Space still folds them. The compact/mobile Sheet keeps the existing scoped list and guild rows unchanged.
