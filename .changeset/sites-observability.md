---
"cognia-next": minor
---

Sites observability is readable. Cloudflare Worker logs render as a scannable table — time, level, message, status, with the original event one click away — and analytics render as headline figures plus request and error-rate trends, with a table view. Both were previously dumped as raw JSON. A result from one Site can also no longer appear under another Site's name.
