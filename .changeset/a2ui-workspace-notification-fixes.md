---
"cognia-next": patch
---

Mini-app timers (Pomodoro, countdown, stopwatch) now count down after Start and Pause/Reset work; the workspace "Agents working" tile opens the list of running agents with links to each run; recurring scheduled tasks keep one updating notification instead of piling up unread rows, and routine maintenance runs no longer notify; macOS reloads the window when its web content process terminates.
