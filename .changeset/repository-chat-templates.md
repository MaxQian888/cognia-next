---
"cognia-next": minor
---

Chat templates can travel in the repository: `.cognia/templates/*.md` appears in the `/` menu for workspaces you trust, so a team shares its phrasing through `git pull`. Any session setup such a file carries is demoted first — it may propose a stricter conversation, never a looser one.
