---
"cognia-next": patch
---

Fix the right-rail workbench focus (full-screen) mode, which had degraded to a clipped 100vw block, and extend the plugin context-panel API with badges, workbench state, and layout control.
