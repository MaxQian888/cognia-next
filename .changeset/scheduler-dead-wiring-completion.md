---
"cognia-next": minor
---

Scheduler: the host bar's choice now binds the whole page (the sidebar, calendar and facet counts follow the paired host the detail pane already acted on), `plan` / `agent-team` / `background-command` / `monitor` refuse with a readable "unsupported on this host" row instead of a raw error, running plugin tasks can be cancelled from the execution list, execution history pages past the first 50 rows, failed pause / resume / run-now and failed bulk actions now say so, plugin progress reports are recorded and can notify, a task can be duplicated, the retry-delay cap and the scheduler's auto-refresh interval are editable, and a recurring-sounding chat message offers to become a scheduled task.
