---
"cognia-next": minor
---

The workspace dock gains a project overview panel, and the task resources panel now shows the evidence a run actually produced — which files and tools a task touched — projected from durable events rather than reconstructed from the transcript. Deleting a project now purges its scoped resources instead of leaving them behind.
