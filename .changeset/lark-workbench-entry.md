---
"cognia-next": minor
---

Feishu workbench entry: `/lark/workbench` signs in with Feishu SSO first, then opens either the paired Host or team access depending on the mode the user chose, with its own workspace picker; it is exempt from the cloud sign-in and first-run onboarding redirects so the Feishu session can be captured.
