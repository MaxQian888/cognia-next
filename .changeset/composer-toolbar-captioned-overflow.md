---
"cognia-next": patch
---

Composer toolbar: the in-box layouts (embedded/rail) now ride the same fold ladder as the detached row — a wide composer spells the whole roster out inline instead of parking it behind "⋯", and the "⋯" disclosure itself is now a captioned settings panel ("Agent mode — Standard") instead of a bare handful of chips floating over the input.
