---
"cognia-next": patch
---

Chat turns that never ran now say so on the message itself: a send waiting for a busy working copy shows as queued (naming what holds it, with Cancel), an external agent that fails to start or finds its process or working copy held elsewhere is marked failed with a localized reason and Retry, in-session plan steps run on external agents now complete or halt like built-in ones, the performance page shares one host metrics lease instead of reporting `device-purpose-limit`, and the sandbox chip no longer claims "Sandbox off" for external-runtime sessions.
