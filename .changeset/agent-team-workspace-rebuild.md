---
"cognia-next": minor
---

Rebuild the Agent Team workspace so a running team is something you can watch and steer rather than something you start and hope about.

Run controls now live on the workspace itself, so starting and stopping a run — and seeing its real state — no longer means leaving for another surface. The task board shows what each teammate is doing and what it produced, the header reports the run's state instead of only the team's name, and the activity stream carries an unread marker so coming back to a long-running team tells you what happened while you were away instead of dropping you at the bottom of a wall of text. Tab navigation keeps its position across a reload.

The mobile Me screen drops its stale Agent Team row: the workspace is desktop-only and the row led nowhere.
