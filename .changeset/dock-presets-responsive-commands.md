---
"cognia-next": patch
---

More dock kernel groundwork: saved layout presets you can name, set as a default, export to a file and import back; a projection that folds a desktop arrangement onto a tablet or phone without losing a panel; and the dock's command surface, with layout undo/redo bound to Ctrl/Cmd+Alt+Z and Ctrl/Cmd+Alt+Shift+Z and the split, float, pop-out and reset actions listed in shortcut settings ready to bind. Still not wired into a visible surface — the chat dock adopts it next.
