---
"cognia-next": minor
---

The desktop app and the phone can name the Cognia Cloud deployment they sign in to. Settings gains a Cloud deployment card (address plus optional certificate fingerprint, checked before use) that replaces the manual Logto form, the sign-in gate re-decides when the deployment changes, a same-origin web build asks its own origin, and the Feishu connector's real target is labelled on the sign-in screen.
