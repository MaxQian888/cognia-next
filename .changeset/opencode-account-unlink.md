---
"cognia-next": patch
---

Removing a discovered OpenCode account from the subscription vault is now labeled "Unlink", and its confirmation makes clear that your on-disk OpenCode `auth.json` is left untouched — Cognia only detaches the pointer it holds.
