---
"cognia-next": minor
---

Project editor VS Code parity pass: the right-sidebar file editor gains a command palette (⌘⇧P via Quick Open `>` mode), go-to-line (`:12`, `:12:3`, `file:7`) inside Quick Open, a second editor group (⌘\\ split, ⌘1/⌘2 focus, per-group tab strips and Monaco instances, tab "Move Editor into Other Group"), full file-tree keyboard navigation (arrows/Home/End/PageUp/PageDown to move, →/← to expand/collapse/step, Enter/Space to open, F2 rename, Delete, type-ahead), a three-button dirty-close dialog (Save / Don't Save / Cancel), automatic tree reveal when jumping to a file, minimap toggle, status-bar indentation + encoding readouts, and Monaco polish (sticky scroll, smooth scrolling/caret, fixed overflow widgets, ⌘B sidebar toggle).
