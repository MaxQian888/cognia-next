---
"cognia-next": patch
---

Layout and hierarchy pass across the app. The settings section header keeps its title on a phone instead of clipping to two characters; the unavailable-capability page centres in the content slot rather than hugging the left edge; /status renders its hero without waiting on a JS animation that could strand it invisible; Discover card names outrank their badges instead of truncating to three characters beside them; /eval, /integrations and /creator each stop drawing four to six identical frames down one column and read as one surface split by hairlines; and the phone summaries on /scheduler and /goals show every number at once instead of hiding half of them behind a horizontal swipe.
