---
"cognia-next": patch
---

Pi agent: session listing and resume now work (host reads Pi's own session store), the model list is available before the first turn via `pi --list-models`, extension-registered providers no longer show as "Not signed in", the credential card lists Pi's model catalog and opens Pi in the terminal for `/login`, and the capability rows for parallel tools, fragmented tool JSON, rate-limit handling and subagents are verified against Pi 0.84.3.
