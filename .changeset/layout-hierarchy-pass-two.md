---
"cognia-next": patch
---

More layout work from the same pass. A settings section embedded in a `/me` route now sheds its card the way it does in the desktop panel, and the master/detail frame twelve sections each drew by hand drops away where the nav rail is a sheet, so a phone gets a flat sheet instead of a box drawn around the whole screen. The Logs search field no longer collapses to three characters, its page header stops printing a two-letter name, and its breadcrumb stops repeating the heading beside it. The subscription web-mode banner stops titling itself with its own body text, the templates catalogue stops saying "built in" twice per row, and the related-sections strip scrolls instead of spending 110px of the first screen on links to other sections.
