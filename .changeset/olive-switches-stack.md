---
"cognia-next": minor
---

Agent Team settings gain a Stacked delivery section: when a run produces several branches in one repository, publish them as a chain of pull requests instead of a flat set. Off by default.
