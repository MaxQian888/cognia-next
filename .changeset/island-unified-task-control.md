---
"cognia-next": minor
---

Turn the Fleet island into a unified task control island: Cognia chats, Agent Team runs, workflow runs and external Claude Code / Codex / OpenCode sessions now share one prioritized list with Minimal, Compact and Expanded presentations. The island window no longer reads app state or runs business commands. The main window pushes a read-only projection and re-validates every action, and a new "Detail in the island" setting controls whether task detail is revealed on click (default), on hover, or never.
