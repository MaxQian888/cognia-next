---
"cognia-next": patch
---

Clicking a plugin's name now opens its details at every window width. Row "⋯" menus open with a plain click, the keyboard, or assistive technology, and never need a hover first. The theme "Active" badge now names the light or dark mode it applies to and marks a theme that's inactive in the current mode. AI Connections shows the external agents that bring their own model access and links to where their credentials are managed.
