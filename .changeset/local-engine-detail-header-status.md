---
"cognia-next": patch
---

AI Connections: the detail header for a local inference engine (Ollama, LM Studio, llama.cpp, …) now agrees with its sidebar row. An enabled or verified keyless engine with no base URL reads "Untested" or "Connected" instead of "Not configured".
