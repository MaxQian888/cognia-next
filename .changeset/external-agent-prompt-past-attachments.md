---
"cognia-next": patch
---

External agents (Codex, ACP agents, a paired host's agents) now receive the question you typed when a message carries an attached document or OCR text. Before, they got only the first attachment's extracted text and never the question. They now get the attachment text followed by the question. Delegation rules now match what you typed instead of a file's contents.
