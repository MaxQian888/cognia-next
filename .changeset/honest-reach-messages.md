---
"cognia-next": patch
---

Three settings controls now say why they are off instead of blaming the platform. The system-scheduler card and the MCP "Test" button explain that the surface needs the desktop app itself rather than claiming the shell is a browser, and the desktop-notification warning is shown only on devices that genuinely cannot display a notification, so phones no longer get warned about a channel they support.
