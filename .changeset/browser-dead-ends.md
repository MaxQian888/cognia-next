---
"cognia-next": patch
---

Recorded browser flows can now be saved to a file — the export menu's download icon previously only ever copied to the clipboard. Replay is disabled for a flow that contains nothing but assertions, which had no actions to replay. The embedded browser also gains a command-palette entry, its first way in from outside the pane itself.
