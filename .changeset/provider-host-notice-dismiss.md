---
"cognia-next": patch
---

The device-local provider settings notice on companion hosts (and the local-engines notice on the mobile shell) is now dismissible. Dismissal is remembered per notice kind on that device, so the banner no longer occupies space on every visit.
