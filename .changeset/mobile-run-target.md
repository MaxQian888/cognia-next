---
"cognia-next": minor
---

A phone can now choose whether a new conversation runs in the local checkout or an isolated worktree, the same choice the desktop has always offered next to the composer. The session environment chip in the chat header also opens on tap, so the branch, base and lifecycle state it describes, and the Manage button inside it, are reachable without a mouse.
