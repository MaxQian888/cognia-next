---
"cognia-next": patch
---

History the model reads mid-answer with `project_history_search` now appears as its own group of source chips on the reply, separate from the mined project claims, each one jumping to the exact turn it came from. The citations are cleared at the start of every turn, so a reply only ever cites what that reply actually read.
