---
"cognia-next": minor
---

Render images as images everywhere in chat: reading an image file now shows the picture (with lightbox, zoom and download) instead of a base64 wall, tool results carrying image/audio/resource blocks keep their content in simplified mode and inside sub-agent trees, plugin tools can return real media, and `take_screenshot` now hands the model an image it can actually see.
