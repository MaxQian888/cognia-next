---
"cognia-next": minor
---

Plans no longer hang when an in-session step's turn fails, never starts, stalls or is cut off by a restart: the step fails with its reason and the plan pauses on it with Retry / Skip / Mark done / Cancel. The approval card also gains Reject (with an optional reason, a real `rejected` status) and Edit (opens the "Write a plan" editor).
