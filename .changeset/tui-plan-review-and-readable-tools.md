---
"cognia-next": patch
---

Give plan review the full document viewport with physical-line scrolling and separate review/action controls. Keep project analysis responses out of automatic plan approval. Wait for external editors to close, review saved revisions before approval, and pass the reviewed content to both current-session and fresh-session execution. Preserve explicit instructions in `/plan refine`.

Improve terminal Markdown and tool-call displays so paths, operations, and structured results remain readable in both fullscreen and scrollback layouts.
