---
"cognia-next": minor
---

The workspace name in the mobile header is now the workspace switcher. It was a read-only label, while the real switcher sat two layers deep in the navigation drawer as a small icon. The Workspace page is also reachable from the Me list, so a phone can see the workspace that owns the repository it was already able to view.
