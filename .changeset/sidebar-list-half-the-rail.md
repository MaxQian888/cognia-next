---
"cognia-next": patch
---

Sidebar: the team band under the conversation list now folds down to the row that names the current scope, handing its rows back to the list (~47% → ~60% of the rail on a 900px window). The fold is persisted, the navigation rows above are never squeezed, and the sidebar's chrome rows drop from 32px to 28px.
