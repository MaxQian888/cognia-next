---
"cognia-next": minor
---

Connector credentials can now be read back and edited from a paired browser or phone. The four keyring commands route over the companion transport behind a short-lived admin lease, a credential the shell has not been granted offers an unlock instead of only "type a new value to replace it", and removing an adapter from a companion destroys its secrets rather than orphaning them.
