---
"cognia-next": minor
---

Rebuild the worktree inventory and the workspace manager: the `/workspace` Environments tab is now a console card with a text filter and per-state chips, every row's secondary actions moved into one labelled overflow menu, and the workspace manager gained a searchable roster, an explicit unsaved-draft state, and a delete confirmation you can back out of. Both surfaces size off the pane they occupy rather than the viewport.
