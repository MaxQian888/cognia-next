---
"cognia-next": patch
---

Discover `?item=` opens a detail sheet (with Start chat / favorite) on every window size and survives back/forward; /squads lists the built-in teams; the status-bar sync chip opens a real device-sync popover with a working Sync now; ⌘K ranks exact and prefix matches (e.g. "memory") above recency hits and tells same-named scheduled tasks apart; the hidden "Pick a character" heading no longer lingers over conversations; an interrupted approval is announced once.
