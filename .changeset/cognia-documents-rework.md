---
"cognia-next": minor
---

Cognia Documents plugin overhaul: structural DOCX import (headings, ordered/unordered lists, real tables, Word comments with resolved state, core title, line breaks/tabs, unsupported-feature tracking), native DOCX export with real comments and tracked changes, interactive review workflows (accept/reject changes, resolve/reopen comments) in a redesigned paper preview, artifact version list/restore, session transcript DOCX export via a registered custom exporter, strict per-operation tool schemas, and full registration cleanup on deactivate.
