---
"cognia-next": patch
---

Composer command pills restyled as ghost tokens: the `/` sigil is repainted as a lightning bolt inside the same character cell (the overlay still mirrors the textarea glyph-for-glyph), the capsule trades its milky fill for a whisper of tint and a hairline ring with real padding and a box-matched corner radius, and pills now tint by command provenance — project commands blue, user commands violet, plugin commands emerald, builtins neutral.
