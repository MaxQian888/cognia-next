---
"cognia-next": minor
---

Spend is now glanceable and explainable. The tray can lead with what you spent rather than only what is left on your plan, a new Capacity Dock pins per-provider gauges to a screen edge on macOS, Windows and Linux, and an opt-in all-tools scope indexes what your other coding agents cost, shown separately and never counted against a Cognia budget. A new Efficiency findings card explains where the money went, using only local deterministic analysis. Underneath, the usage ledger and the daily budget projection now have a single writer, so a retried turn can no longer be billed twice.
