---
"cognia-next": patch
---

The Template Studio can confine a template to a workspace or share it again, its tabs and selection live in the URL, a phone can reach `/templates` and chat-template settings from the home screen and the Me list, and `/templates`, `/discover` and `/agent-runs` no longer collapse to a blank strip on a phone.
