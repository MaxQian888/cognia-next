---
"cognia-next": patch
---

SRE agent panel: an incident timeline the agent already ran through the validator can be adopted with one button, carrying its verdict with it, instead of being retyped by hand. Evidence the agent gathered from traces and metrics can be pinned alongside its log evidence, and the button's count and its action are now derived from one place, so "pin 7" pins those 7. The "agent observed N times" line counts only the queries that actually pinned evidence, so validating a timeline no longer inflates it.
