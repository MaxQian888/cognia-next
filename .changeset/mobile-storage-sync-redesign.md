---
"cognia-next": minor
---

Redesign the mobile Storage and Sync pages: one usage bar split by category with health and persistence inline, a single refresh for the whole page, per-category rows with icon tiles, a Manage block linking to backups instead of a second file list, and a sync summary that leads with a verdict and pulls failed tables into a "Needs attention" group. Both pages now adapt to the panel width (two columns on tablets).
