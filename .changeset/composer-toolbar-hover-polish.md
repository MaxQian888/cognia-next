---
"cognia-next": patch
---

Unify hover feedback across the composer toolbar: every control now shows a styled tooltip and the same muted hover state, and the context-usage card waits for a deliberate 500ms hover instead of popping open on a pointer sweep.
