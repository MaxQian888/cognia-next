---
"cognia-next": patch
---

Say why a bot cannot do something instead of hiding the control. Reply quoting, the usage-status badge tier and the Inbox "load earlier" bar now render disabled with the reason and, where one exists, the next step — re-authorize, switch transport, upgrade the upstream, or turn a setting on. A bot whose stored status tier it cannot publish now says so and offers a one-press repair, instead of displaying a tier it was never set to while publishing nothing.
