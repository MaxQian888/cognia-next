---
"cognia-next": minor
---

Finish converging the chat right-side dock onto one shell. Opening the embedded browser no longer throws away the artifact you were looking at — it is now a panel alongside the preview, so one click returns you. The dock also stops occasionally snapping back out of the surface you just picked, because panel selection now has a single owner instead of two states correcting each other. The "wide" button finally reaches the full width the workspace allows (previously it stopped at the narrower artifact limit), and opening an artifact on a phone gives you the touch editor instead of the desktop code editor. The old pre-workbench dock has been removed.
