---
"cognia-next": patch
---

Composer: align the in-box action row. The attach/voice icons, the status chips and the send button now share one centre line (they were bottom-aligned at three different heights, so the icons rode above the chip text), the paperclip matches the 32px control size beside it, the "+" glyph lines up with the first character of the message above it, and the embedded/rail/focus toolbars adopt the wide row's zoning — controls start where the icons end, ambient status and the "⋯" disclosure are pinned right — instead of packing everything against the send button behind a half-composer-wide gap.
