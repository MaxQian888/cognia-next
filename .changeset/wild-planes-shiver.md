---
"cognia-next": minor
---

Wire diagnostics-first safe mode: the desktop app now persists recovery state across restarts, enters a diagnostics shell after a second unhealthy start of the same build within ten minutes, and bounds renderer reloads and subsystem restarts instead of retrying forever.
