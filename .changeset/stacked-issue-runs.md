---
"cognia-next": minor
---

Running a GitHub-linked issue can now stack its pull request on another issue's branch instead of the base branch, so each review shows only its own change. The parent is recorded in the local checkout when the delivery container references one.
