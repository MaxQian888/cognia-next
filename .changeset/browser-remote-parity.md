---
"cognia-next": patch
---

The cloud browser now has the same collapsible tools strip as the built-in preview, so its console and network output are visible there for the first time. Its "reuse Chromium sign-in" button is shown disabled with an explanation instead of quietly missing — that feature reads your local browser's saved logins, which a cloud browser cannot use.
