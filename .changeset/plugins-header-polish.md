---
"cognia-next": patch
---

Polish the /plugins page header. The active section now sits beside the title as a badge with its rail icon instead of a dangling "· Library" fragment after a truncated description, the page description is shortened to a single scannable line in both locales, and Sync registry shares the outline variant with Check for updates so the action row carries one primary and two equal-weight secondaries.
