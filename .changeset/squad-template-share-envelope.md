---
"cognia-next": patch
---

Sharing a squad template via link works again, and a template row that has only a draft says so. The row's platform status now carries the release envelope a share link needs instead of only its version string, so the share button no longer has to re-read the library per row, and a row with nothing published renders disabled with the reason rather than silently doing nothing.
