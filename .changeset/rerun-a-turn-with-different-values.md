---
"cognia-next": minor
---

Re-run a past message with different values. A turn written from a template now offers a re-run action that loads it back into the composer with its parameter chips filled in, ready to change one and send again — the sent message has the values substituted in, so it could not be varied before. It refuses over a non-empty composer rather than replacing what you are writing.
