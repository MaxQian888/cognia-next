---
"cognia-next": minor
---

The first-run flow, the pairing screen and the in-app setup reminders now share one guide design: the same window bar, narrative panel, progress row, headings and entrance animations everywhere. Setup reminders now show what is actually missing right now: the finish-setup bar and the provider page banner disappear once a model is connected anywhere, and they no longer blame a missing runtime on a machine that has one. Their buttons go straight to the step that fixes the gap. Settings → Discover shows the same live setup status. The recommended path's final screen now says it is your turn and no longer asks the style-pack question.
