---
"cognia-next": minor
---

Rebuild the `/memory` workspace on the shared feature shell: one header band (tabs, recall-status chip, corpus summary, quick views, search, Filter, Display) over a list that now owns ~84% of the viewport instead of a third. Archiving is the primary row action with permanent delete demoted behind a confirmation, the inspector renders a memory's evidence and audit history rather than counting it, and the External agent memory tab is translated for the first time and finally shows OpenCode and Pi files alongside Claude Code and Codex.
