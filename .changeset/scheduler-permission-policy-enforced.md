---
"cognia-next": minor
---

The scheduler's permission settings now actually apply. Agent auto-creation, the per-source task limit, the script-task switch, and the per-type confirmation list are enforced on every write an agent or plugin makes, and the task limit is counted per source rather than across your whole schedule.
