---
"cognia-next": patch
---

When a browser preview is already open somewhere else in the app, the second one now says so and offers to take it over, instead of spinning for 20 seconds and then showing an empty panel. Its toolbar is disabled while it waits, and it no longer mirrors the other panel's console and network output or competes for element picks.
