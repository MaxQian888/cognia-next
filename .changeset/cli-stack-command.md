---
"cognia-next": minor
---

The CLI gains `/stack` — list, record a parent, verify, restack, and push stacked branches from the terminal — and `/pr` now bases a pull request on the recorded stack parent instead of flattening it onto the trunk.
