---
"cognia-next": minor
---

Lark unified identity and dual-entry surfaces (ADR-0091): fail-closed Feishu principal registry with per-account isolation, actor authorization on high-privilege card callbacks (audit-first rollout), web SSO with single-use authorized entry links, reconciled Chat Tab and group menus, bot-menu hardening (unknown keys answer a fixed notice instead of reaching the model), message-shortcut import (server-verified, idempotent) and plus-menu new task, with per-adapter feature flags, metrics, and audits — all surfaces default off.
