---
"cognia-next": patch
---

Restyle the desktop status bar as a quiet line: drop the muted fill band for a hairline top edge, flatten per-segment hover tiles into a colour-only change, and tighten type and spacing so the bar reads as one continuous line of status rather than a row of boxes.
