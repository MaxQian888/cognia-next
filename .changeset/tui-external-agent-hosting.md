---
"cognia-next": minor
---

Let `cognia-agent chat` host Codex, Claude Code, and other executable external-agent presets with `--backend`. External agents run through the existing ACP/Codex protocol plane inside the CLI's strict native sandbox, stream into the existing TUI cells, reuse its permission overlay, and expose backend availability through `/doctor`.
