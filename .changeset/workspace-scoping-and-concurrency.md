---
"cognia-next": minor
---

⌘K now searches the current workspace by default and says which one, with one click to widen; skills a workspace switched off rank below the ones it uses instead of disappearing. Schedules belong to the workspace they were set up for. Notifications name the workspace they came from when it is not the one on screen.
