---
"cognia-next": minor
---

Rebuild the system-wide selection toolbar: the overlay window is now sized from its own content instead of a hard-coded 360×44 box, which restores the capsule's drop shadow, removes the transparent dead zone beside it, and stops the language menu teleporting the toolbar up the screen. Adds icon-first buttons that expand on hover, enter/exit animation anchored to the selection, hover-to-keep-open, two new actions (add to memory, read aloud), `⌥⇧1`–`⌥⇧6` shortcuts, a keyboard-navigable language list where Escape closes the list before the toolbar, and a default translation language setting.
