---
"cognia-next": minor
---

Subagents: `dispatch_agent` gains `cancel`, multi-run `collect` with a wait `timeoutMs`, and a per-call `model` override. Fan-out concurrency is now configurable (app: Settings, Sub-agents, Nested sub-agents. CLI: `subagentMaxConcurrent`).
