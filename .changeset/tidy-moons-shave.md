---
"cognia-next": patch
---

A conversation started in a browser paired to a server now renders its own history again. The host was asked for a transcript it had never stored, and answered in a way the browser could only show as an error.
