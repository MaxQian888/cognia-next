---
"cognia-next": minor
---

Rebuild the `/pair` screen on the first-run shell's two-pane language: an opaque full-window surface (the desktop wallpaper used to show through under the body text), a brand panel with a state-driven diagram of the client, the Host and the link between them, and a single-focus body. A decoded invitation now collapses into a target card naming the Host, its version and its expiry, with the raw `cgnp3` string editable behind a disclosure; a failure shows one cause, one next step and one button, with the remaining remedies and diagnostics one disclosure away. The browser flow also regains loopback discovery — it now reports whether a Host is listening on this machine and, when one refuses the tab, hands over the exact origin to allowlist.
