---
"cognia-next": minor
---

Composer skill attach entry now opens a right-side flyout from the `+` menu row instead of a modal dialog — picks toggle inline without a context jump, and the menu stays open behind it.
