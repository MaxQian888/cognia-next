---
"cognia-next": minor
---

Workflow subgraphs can be grouped and inserted as one reusable unit instead of being rebuilt node by node, and the workflow library gains search across name, description and the node kinds a workflow contains.
