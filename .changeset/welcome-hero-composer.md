---
"cognia-next": minor
---

The welcome screen now has a live input box under the greeting: type a first message and the conversation is created around it, instead of clicking "New chat" first. It is the real composer, so attachments, slash commands and @-mentions work from the first keystroke — including attachments staged before the session exists. Also fixes the display name and rich/minimal style from Settings → Personalization, which were being saved but never read by the welcome page.
