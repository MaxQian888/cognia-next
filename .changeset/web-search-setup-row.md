---
"cognia-next": minor
---

When web search cannot run — the web-tools switch is off, the master search switch is off, or no provider is configured — the `+` menu row now opens a small setup card naming the blocker with a button that jumps to the settings section that fixes it, instead of sitting as a dead disabled toggle.
