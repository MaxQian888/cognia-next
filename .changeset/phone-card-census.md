---
"cognia-next": patch
---

Second phone layout pass over the card-heavy mobile components: the /me stats become one hairline strip instead of four cards, the backup nudge is a proper status alert, the workflow, goal and remote-session lists join into one surface each, the Computer Use host panel stops nesting frames three deep, and the twin profile panel matches the sources panel beside it.
