---
"cognia-next": minor
---

Chat template parameters can now hold a reference: declare one as a workspace file, a team member or a subagent and it opens the same picker `@` does. Declared choice lists, multi-line fields and optional parameters work too.
