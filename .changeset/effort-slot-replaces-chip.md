---
"cognia-next": patch
---

The composer no longer shows two thinking-depth controls at once: a plugin dial now contributes to the new `chat.input.effort` slot and replaces the built-in effort chip instead of sitting beside it (the Tactical Mind Dial moves there). The plugin path now offers the external agent's own published ladder (Pi's `xhigh`/`max`) and wakes when that ladder arrives, and the built-in chip resolves an agent-owned app default instead of hiding itself. The toolbar row gets visible zone dividers and a risk-keyed shield icon on the permission chip, so every per-turn chip leads with a glyph.
