---
"cognia-next": minor
---

One 40px bar above the workspace: the title bar renders in the web shell too, grows to the shared column-header height (macOS traffic lights recentred), and hosts the conversation rail's, chat pane's and artifact dock's headers in three zones aligned to the columns beneath — so the chat workspace sits under a single row instead of a title bar plus one header per column. Column headers still draw their own row on mobile and outside the chat workspace.
