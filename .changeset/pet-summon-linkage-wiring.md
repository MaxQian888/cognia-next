---
"cognia-next": minor
---

Desktop pet: summoning the pet now switches it on and reuses the saved overlay size and position, from every entry point (the global hotkey, the tray toggle, ⌘K, the Settings switch and the agent's `pet_show`), instead of raising an unresponsive or even invisible window. Fresh Attention Radar reports reach the pet as a happy moment with an "Open Insights" button on the bubble, in the app and on the desktop overlay. ⌘K gains "Toggle desktop pet" and "Open pet panel", and feed/play/pet/sleep/clean/treat can be bound as global shortcuts that answer with a notification while the pet is off. `/pet` and the agent's `pet_care` now report cooldowns and a switched-off pet honestly, the pet no longer appears in the web or mobile navigation, ⌘K, Settings or agent tools where it cannot run, the title bar's Capture button opens the capture settings, and a new user's first goal progress, first workflow run and a twin's first distill now reach the pet.
