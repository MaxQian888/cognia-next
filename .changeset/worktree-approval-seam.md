---
"cognia-next": patch
---

Worktree and workspace actions now work from a paired phone or browser. Every task-workspace write needed a host approval lease it was never given, so the inventory listed environments while every button on it failed. Disabled controls now say whether the host never offered the action, the device is one permission away, or the connection dropped.
