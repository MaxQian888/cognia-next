---
"cognia-next": minor
---

Import issues from a CSV, a JSON export or a Markdown task list, with a preview, a container and cycle choice, and re-import that skips rows already brought in. The board header gains Import and Sync now, Sync now covers every external binding and reports what it imported, updated, pushed or left awaiting approval, and a conflicts panel lists every field the sync engine had to decide so a person can keep the other side instead.
