---
"cognia-next": minor
---

Plugins can now read a Squad from Python, start, pause, resume and stop a run through `ctx.team`, instantiate a Squad template, and bind a conversation to a Squad. The API is published as `@cognia/plugin-sdk/api/team`.
