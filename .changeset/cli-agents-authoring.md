---
"cognia-next": minor
---

CLI `/agents` is now a complete sub-agent console: `run` shows the child in the live panel (with `--bg` to park it as a background run), `stop <runId>` is wired, `new`/`edit`/`rm` author project agent files, the panel animates running rows and marks estimated token counts with `~`.
