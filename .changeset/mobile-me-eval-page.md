---
"cognia-next": minor
---

Added `/me/eval` so the Agent evaluation defaults (judge model, run k, gate thresholds, cost guard) are reachable from the phone. Shortcuts, the desktop pet and the external bridge are the sections still without a mobile entry.
