---
"cognia-next": patch
---

Smooth out the chat side panel's collapse and fix it at narrow widths. Collapsing or expanding the panel now slides it out of the way instead of squashing its contents for the length of the animation, and the drag divider fades along with it rather than popping in and out. Dragging the panel narrow no longer cuts off the artifact list's batch-select button — the type and status filters shrink to icons, with a dot marking a filter you still have applied. The list also stops clipping its last row, which was worst while batch mode was on. Both the conversation list and the side panel now collapse at the same pace and both respect your motion-speed setting.
