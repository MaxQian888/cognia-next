---
"cognia-next": minor
---

Sites: a new Access tab lets the Site owner name editors and deployers. The permission model was already enforced on every mutation but had no way to be edited, so every Site's collaborator lists were permanently empty. The publish flow's first step also now says whether a provider token is verified on this machine, stored on a different one, rejected, or absent — instead of staying blank until the first resource was provisioned.
