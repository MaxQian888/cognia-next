---
"cognia-next": minor
---

Sites: builds now record what they printed. Each phase announces itself as it starts and finishes, so a multi-minute build shows progress instead of a bare spinner, and the full install and build output — trimmed and credential-redacted — is kept per version and readable from the versions list. Previously a successful build's output was discarded entirely and a failed one was reduced to a single line.
