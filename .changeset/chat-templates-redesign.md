---
"cognia-next": minor
---

Chat templates redesigned: the settings page is now a searchable master-detail library where the selected template's message renders with its `{{slots}}` as clickable fill-in chips — the same gesture and popover the composer uses, including resource pickers and unresolved-value states. Editing collapses parameter declarations behind a disclosure, and a new AI assist row can draft a template from an intent, improve the body, or suggest slot declarations (PII-gated, cancellable, reviewed before saving).
