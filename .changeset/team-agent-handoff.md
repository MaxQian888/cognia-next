---
"cognia-next": minor
---

Agents in a team room can hand each other the floor. A member that names a teammate in its reply now passes the turn to them without the user having to push every round, bounded by a new per-team auto-round budget (off by default), the existing response cap, and a limit of two turns per member per message.
