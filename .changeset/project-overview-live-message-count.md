---
"cognia-next": patch
---

The project overview panel's "Conversations" and "Messages" stats now count the conversations the workspace's chat list shows and the messages in them, updating as they change. Before, Messages always showed 0, and Conversations left out conversations that belong to no workspace.
