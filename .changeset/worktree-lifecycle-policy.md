---
"cognia-next": minor
---

Settings now shows the worktree lifecycle policy: how many worktree directories the machine keeps, how long snapshots are retained, and the content store budget. These rules already governed the maintenance run but had no UI at all, so there was no way to see or change what that run would reclaim.
