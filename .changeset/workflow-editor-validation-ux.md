---
"cognia-next": patch
---

Workflow editor and library: "Clear filters" now also clears the search (and the empty state names the query that hides results), nodes lead with their configured content and name missing required fields, Problems rows focus the exact invalid field, issue badges and the save toast track the current params, and plan nodes accept the new `rejected` status and `step_skipped` event.
