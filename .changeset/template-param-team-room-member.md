---
"cognia-next": minor
---

A chat template parameter can now pick a member of a team room. Declare it as "Team room member" and, in a character-team conversation, the parameter opens a picker over that room's members and fills in the exact `@Name` the turn router matches; a remembered member who has since been renamed shows as unresolved instead of sending a mention that reaches nobody.
