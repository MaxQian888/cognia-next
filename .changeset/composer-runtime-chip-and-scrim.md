---
"cognia-next": patch
---

Composer: the runtime chip now lists the external agents itself (no more "External (none selected)" dead end), the wide toolbar stops collapsing Agent mode / sandbox into "⋯", and the bar behind the input fades with the wallpaper instead of painting an opaque slab over it.
