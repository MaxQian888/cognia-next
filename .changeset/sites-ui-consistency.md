---
"cognia-next": minor
---

Sites console visual pass: panels now follow the app's surface and radius system so they match their neighbours over a wallpaper; multi-column layouts size to the resizable pane rather than the monitor, so a narrow pane no longer tries to seat five columns; the overview carries a stat strip; loading, "no Sites yet", and "no matches" are three distinct states instead of a blank pane; every field in the new-Site dialog is labelled and shows the source directory it would build from; detaching a custom domain asks first; and the phone shows a read-only list of whatever Sites that device knows about, with an explanation, instead of a bare "desktop only" card.
