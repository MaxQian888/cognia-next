---
"cognia-next": minor
---

Workflows gain stacked-branch nodes: list the chains a repository records, set a branch's parent, validate that every layer really contains its parent, restack, and push the whole chain with a lease.
