---
"cognia-next": minor
---

Pro IDE (code-server) is available from a browser, not only the desktop app. A browser paired with a host can start that host's workbench and, on the host's own machine, edit in it from the right rail — with terminal links, review jumps and agent file-follows landing in that workbench rather than the read-only viewer. Anywhere else it says where the workbench can be opened instead of claiming the platform is unsupported.
