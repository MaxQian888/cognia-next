node_modules
.next
out
build
coverage
dist
target
src-tauri/gen
pnpm-lock.yaml
*.min.js
*.min.css
public/monaco
# Serwist owns these generated, minified service-worker bundles.
public/sw.js
public/swe-worker-*.js
# Integrity-pinned models.dev shards are compact output owned by the sync generator.
public/catalog/models-dev/providers
# Vendored skill content — managed by the skill plugin, not hand-edited.
.agents/skills
.claude/skills
# Bundled built-in skills + their generated catalog. Prettier-reformatting the
# SKILL.md sources would desync the committed generated catalog; the
# `skills:build` codegen (pnpm skills:build) owns both.
skills/built-in
lib/skills/built-in-catalog.generated.ts
# esbuild owns this generated CommonJS bundle; formatting it would make the
# committed artifact diverge from `pnpm plugin-convert:bundle`.
crates/cognia-cli/assets/plugin-convert.cjs
# Same arrangement for the workflow-app embed bundle: esbuild writes it
# (`pnpm workflow-app:embed:build`), so Prettier reformatting it would put the
# committed file permanently out of step with the generator — and lint-staged
# would reformat it again on every commit that touches it.
public/cognia-workflow-app.js
# The two browser-plane catalogs are generator output compared byte-for-byte by
# their own drift checks (`pnpm lucide:check`, `pnpm plugin:builtin:build`).
# `generate-lucide-catalog.mjs` writes a single unindented line on purpose —
# 1.6k icons, read only by the bundler — so Prettier reformatting either file
# would make its check report drift on a clean tree.
lib/icons/lucide-catalog.generated.json
lib/plugin/core/browser-builtin-assets.generated.json
# Companion API specs and CLI catalog are canonical generator output. Prettier
# rewrites compact schema arrays and makes `companion-api:check` report drift.
docs/api/mobile-companion-api.openapi.yaml
docs/api/headless-service-api.openapi.yaml
crates/cognia-cli/assets/host-command-catalog.json
# The same generator writes these three, and they were missed when the block
# above was written — so every commit reformatted them and `companion-api:check`
# reported drift on a tree whose sources and outputs were both clean. The drift
# was pure whitespace (identical once minified), which is exactly why it went
# unfixed: it looked like noise rather than a broken gate.
protocol/companion-request-schemas.json
cli/src/serve/headless-contract-identity.ts
cli/src/serve/fixtures/bridge-frames.json
# Missed for the same reason, and it bites harder than whitespace: this file is
# `include_bytes!`d by `cognia-headless-contract`, so a reformat that trips
# `companion-api:check` is sitting in the binary that validates every
# device-plane request.
crates/cognia-headless-contract/assets/device-plane-overrides.json
# Generated i18n message bundles — built from the per-namespace split sources
# by `pnpm i18n:build`. Prettier collapses the build's expanded arrays, which
# desyncs the committed bundle from `i18n:build:check`; the codegen owns format.
i18n/messages/en.json
i18n/messages/zh-CN.json
# The Support Agent corpus is compact generator output verified byte-for-byte
# by `pnpm support:docs:check`; preserve the generator-owned representation.
lib/support-agent/support-docs.generated.json
# Helm chart templates — Go templating (`{{- include ... }}`) is not valid
# YAML, so Prettier's YAML parser rejects the file outright. Helm owns them.
services/diagnostic-server/deploy/helm/cognia-diagnostics/templates
# CommonJS mocks for Jest; intentionally hand-written, not Prettier-shaped.
__mocks__
# Capacitor mobile shell — separate package, has its own conventions.
mobile
# IDE configuration files — not project source.
.vscode
# Fumadocs MDX content — Prettier's MDX support is non-idempotent on
# `<Steps>` / `<Step>` whitespace (prettier(input) !== prettier(prettier(input))).
# The docs workspace owns its own formatting.
docs/content
